# 작업 맥락 기록

## 2026-07-16

### 확인한 현재 상태

- 저장소는 실행형 애플리케이션이 아니라 Java Spring Boot 기반 사내 업무관리 시스템을 직접 구현하도록 돕는 Markdown 워크북과 코드 조각으로 구성되어 있다.
- `practice/README.md`에는 40개 기술 챕터의 순서와 사용법이 잘 정리되어 있다.
- `feature-implementation-workbook.md`, `problems.md`, `answers.md`, `starter/`, `hard/`가 구현 연습에는 충분한 구조를 제공한다.
- 작업 시작 시 Git 작업 트리는 깨끗했다.

### 판단

- 사용자가 말한 “취업을 위한 워크북 형태”에 맞추려면 기존 기술 문제를 새로 만드는 것보다 취업 목표, 주차별 결과물, 포트폴리오 증거, 면접 답변, 지원 준비 상태를 한곳에서 추적하는 진입 문서가 필요하다.
- 현재 저장소 루트에 `README.md`가 없어 처음 방문한 사용자가 문서 읽는 순서와 최종 산출물을 바로 이해하기 어렵다.
- 기존 워크북의 장점은 유지하고, 새 통합 워크북과 최소한의 링크 수정으로 목적을 분명하게 만드는 것이 가장 단순하다.

### 구현 범위

- 루트 `README.md`를 추가해 프로그램 목적, 대상, 사용 순서, 최종 산출물과 완료 기준을 안내한다.
- `practice/job-preparation-workbook.md`를 추가해 목표 설정, 진행 기록, 구현 증거, 트러블슈팅, 면접 답변, 포트폴리오 및 지원 준비 체크를 제공한다.
- `practice/README.md`의 사용 방법과 완료 기준을 취업 결과물 중심으로 보완한다.
- 기존 문제, 정답, 코드 조각의 기술 내용은 요청 범위를 벗어나므로 변경하지 않는다.

### 검증 기준

- 새 문서의 상대 링크가 실제 파일을 가리킨다.
- Markdown 제목과 표 구조가 깨지지 않는다.
- 취업 준비 사용자가 “무엇을 공부하고 무엇을 남겨야 하는지”를 저장소 첫 화면에서 이해할 수 있다.

### 구현 결정

- 루트 `README.md`를 저장소의 첫 진입점으로 추가했다.
- 기존 기술 워크북을 반복하지 않고 `practice/job-preparation-workbook.md`에서 목표 직무, 채용 공고 기준, 기능별 증거, 트러블슈팅, 포트폴리오, 면접, 지원 직전 점검을 연결했다.
- 기존 “한 줄로 답하면 졸업” 기준은 취업 결과물 기준으로 약하므로 개념 점검으로 낮추고, 실행 코드·테스트·검증 명령·커밋을 챕터 완료 조건으로 명시했다.
- 기존 코드 조각이 컴파일 대상이 아니라는 사실을 루트 문서에서도 강조해 완성 애플리케이션으로 오해하지 않도록 했다.
- 현재 저장소에 없는 `bank-system/practice`와 `.claude/.../spec-index.md` 참조를 제거하고, 저장소 안의 PRD/TRD·정답 문서로 확인 경로를 통일했다.

### 검증 결과

- 상대 Markdown 링크와 표 열 수 검사 통과.
- 변경 문서의 후행 공백 검사 통과.
- 존재하지 않는 기존 내부 참조 제거 검사 통과.
- 실행 가능한 애플리케이션과 빌드 설정이 없는 문서형 저장소이므로 코드 테스트와 빌드는 실행 대상이 아니다.

## 2026-07-16 문서 보완과 히스토리 배포

### 사용자 요청

- 취업을 위한 워크북 형태의 프로그램이라는 목적에 맞춰 저장소를 다시 점검하고 문서를 보완한다.
- 실제 변경 이력을 보여주는 `history.html`을 만든다.
- 변경사항을 커밋하고 원격 저장소에 푸시한다.

### 추가 확인

- 현재 브랜치는 `main`이며 작업 시작 시 원격 `origin/main`보다 기존 커밋 1개가 앞서 있다.
- 실제 Git 이력은 2026-05-24 최초 PRD/TRD·40개 챕터 추가부터 2026-07-16 취업 포트폴리오 흐름 정리까지 5개 커밋이다.
- 문서에서 워크북 프로그램 자체와 워크북으로 구현할 사내 업무관리 시스템이 명확히 구분되지 않아 사용자가 저장소를 완성 애플리케이션으로 오해할 수 있다.
- GitHub CLI가 설치되어 있지만 인증 토큰이 유효하지 않다.
- 현재 실행 환경에서 `github.com`의 DNS 조회가 실패해 원격 확인과 푸시에 실패할 가능성이 있다.

### 구현 범위

- 루트 README와 PRD/TRD에서 “워크북 프로그램”과 “구현 대상 시스템”의 역할을 구분한다.
- 문제·정답·기능 구현·진행 기록 문서가 동일한 학습 루프와 완료 증거를 안내하도록 정리한다.
- 루트 `history.html`에 실제 커밋 타임라인, 현재 구성, 문서 링크를 제공한다.
- 기존 기술 문제와 코드 조각의 내용은 유지한다.

### 문서 보완 결정

- 루트 README에 프로그램 정의, 문서 유지 원칙, 변경 히스토리 진입점을 추가했다.
- PRD/TRD 제목과 문서 목적을 워크북 관점으로 바꾸고, 워크북 사용자와 구현 대상 시스템 사용자를 구분했다.
- 문제, 정답, 기능 구현, 통합 워크북이 모두 `목표 선택 → 직접 풀이 → 구현 → 검증 → 증거 기록` 순서를 안내하도록 맞췄다.
- 기존 기술 요구사항과 코드 예시는 학습 자산이므로 내용 자체는 변경하지 않았다.
- `history.html`은 외부 라이브러리 없이 단독으로 열리는 반응형 문서로 만들고, 실제 Git 커밋 5개와 이번 변경 단계를 타임라인으로 정리했다.

### 전체 문서 전수 보완

- 저장소의 Markdown 문서 17개를 모두 확인했다.
- 보안, 포트폴리오 문서화, Thymeleaf, 패키지 구조, 비즈니스 규칙, 트러블슈팅, 면접 워크시트에 워크북 프로그램에서의 역할과 완료 후 남길 증거를 추가했다.
- Hard 모드도 빈칸 작성에서 끝나지 않고 실행 프로젝트 구현과 테스트·Git 기록으로 이어지도록 수정했다.
- 내부 작업 문서인 `checklist.md`와 `context-notes.md`는 진행 및 결정 기록 용도로 계속 유지한다.

### 검증 결과

- `git diff --check` 통과.
- 전체 Markdown 17개가 이번 목적 점검에 포함됐음을 확인.
- 전체 Markdown 상대 링크와 표 열 수 검사 통과.
- `history.html`의 로컬 링크, 한국어 헤더 주석, doctype, 언어 메타데이터 검사 통과.
- `history.html`의 태그 중첩, 중복 ID, CSS 중괄호 검사 통과.
- `history.html`에 연결한 실제 Git 커밋 5개의 객체 존재 검사 통과.
- Chrome 헤드리스 화면 캡처는 실행 환경의 샌드박스 제한으로 종료되어 파일 구조 기반 검증으로 대체했다.

### 배포 결과

- 문서 보완 커밋 `3cfbcc7 docs: 취업 워크북 문서와 히스토리 정비`를 생성했다.
- `git push origin main`이 성공해 `origin/main`이 `4379abe`에서 `3cfbcc7`까지 갱신됐다.
- 기존에 로컬에만 있던 `f08763e` 커밋도 이번 푸시에 함께 반영됐다.
- GitHub CLI 인증 상태는 유효하지 않았지만 Git 원격 자격 증명은 정상 동작했다.
- 사용자는 Pull Request 생성을 요청하지 않았으므로 `main` 푸시까지만 수행했다.

## 2026-07-16 취업 성과 추적 고도화

### 사용자 요청

- 프로그램의 목적을 취업을 위한 워크북으로 다시 확인하고 필요한 내용을 수정한다.
- `history.html`을 만들거나 최신화하고 Git 원격 저장소에 푸시한다.
- 프로그램 안의 모든 문서를 목적에 맞게 수정·보완한다.

### 확인한 현재 상태

- 저장소는 실행형 애플리케이션이 아니라 40개 기술 챕터, 난이도별 코드 조각, PRD/TRD, 진행 기록으로 구성된 문서형 워크북이다.
- 작업 시작 시 `main`, `origin/main`, `HEAD`는 모두 `c5e3939`로 일치했고 작업 트리는 깨끗했다.
- 기존 문서는 워크북과 구현 대상 시스템을 구분하고 있으나, 채용 공고 요구사항부터 기능·코드·테스트·문서·면접 답변까지 이어지는 공통 추적 키가 없다.
- 단계별 완료 기준이 최종 졸업 기준에 집중되어 있어 사용자가 현재 단계에서 다음 단계로 넘어가도 되는지 판단하기 어렵다.
- `history.html`은 실제 커밋 `3cfbcc7`, `c5e3939`를 링크된 완료 이력으로 반영하지 않아 최신 Git 이력과 일치하지 않는다.

### 설계 결정

- 신규 업무 기능이나 실행 코드는 추가하지 않고 문서형 워크북의 사용성만 보완한다.
- 공통 증거 ID를 `E-{FR ID}` 형식으로 두고 채용 공고 요구사항, 코드, 테스트, 문서, 면접 답변을 같은 행에서 추적한다.
- 준비, 설계, 구현, 검증, 지원의 5단계에 최소 통과 기준을 두어 학습량이 아니라 증거로 다음 단계 진입을 판단한다.
- 모든 문서에는 긴 설명을 반복하지 않고 해당 문서에서 만들 증거와 통합 워크북에 기록할 위치만 짧게 추가한다.
- `history.html`은 이번 변경 전까지 확정된 7개 커밋을 실제 링크로 표시하고, 이번 작업은 커밋 전까지 “현재 업데이트”로 구분한다.

### 검증 기준

- 모든 Markdown 상대 링크가 존재한다.
- Markdown 표의 열 수와 후행 공백이 정상이다.
- 공통 증거 ID와 5단계 통과 기준이 루트 안내, PRD/TRD, 실습 문서에서 같은 의미로 사용된다.
- `history.html`의 커밋 해시가 실제 객체로 존재하고 HTML 태그, 중복 ID, CSS 괄호가 정상이다.
- `git diff --check`가 통과한다.

### 문서 구현 결과

- 루트 README와 통합 워크북에 준비·설계·구현·검증·지원의 5단계 통과 기준을 추가했다.
- 기능, 문서·데모, 트러블슈팅, 면접 답변을 각각 `E-FR-*`, `E-DOC-*`, `E-TS-*`, `E-INT-*`로 추적하도록 했다.
- 채용 공고 3개 기록표, 기능·비기능 증거 인덱스, 주간 회고, 지원 현황 표를 통합 워크북에 추가했다.
- PRD/TRD, 기능 구현, 문제, 해설, Hard 모드, 7개 서술형 Starter 문서가 같은 증거 ID와 완료 산출물 흐름을 사용하도록 연결했다.
- 기존 코드 조각과 기술 해설은 변경하지 않고 문서형 워크북의 진행·증거 기록 구조만 보완했다.

### 1차 검증 결과

- `git diff --check` 통과.
- Markdown 18개 파일의 상대 링크 검사 통과.
- Markdown 18개 파일의 표 열 수 검사 통과.
- 공통 증거 ID와 단계별 통과 기준이 주요 진입 문서와 워크시트에 포함됐음을 검색으로 확인했다.

### 히스토리 최신화

- 취업 증거 추적 체계 보완 커밋 `4010870`을 최신 프로그램 단계로 추가했다.
- 기존 문서 정비 커밋 `3cfbcc7`과 배포 결과 기록 `c5e3939`를 실제 Git 링크로 추가했다.
- 확인된 8개 Git 단계, 40개 학습 챕터, 5단계 취업 준비 흐름을 요약 카드에 반영했다.
- 준비·설계·구현·검증·지원 흐름과 기능 구현·면접 워크북 링크를 프로그램 지도에 추가했다.

### 2차 검증 결과

- `history.html`의 로컬 링크 9개와 중복 ID 검사 통과.
- `history.html`의 HTML 태그 중첩과 CSS 중괄호 검사 통과.
- `history.html`에 연결한 실제 Git 커밋 8개의 객체 존재 검사 통과.
- `git diff --check` 통과.
- 브라우저 화면 제어 호출 수단이 현재 세션에 제공되지 않아 렌더링 캡처는 실행하지 못했고, 반응형 CSS와 정적 구조 검사로 대체했다.

### 커밋과 배포 결과

- 문서 전반의 취업 증거 추적 체계 보완 커밋 `4010870 docs: 취업 증거 추적 체계 보완`을 생성했다.
- 변경 히스토리 최신화 커밋 `0d96064 docs: 워크북 변경 히스토리 최신화`를 생성했다.
- 최종 검증에서 Markdown 18개 파일의 표와 상대 링크, `history.html`의 로컬 링크 9개와 Git 커밋 링크 8개가 모두 통과했다.
- 별도 `git fetch origin main`은 실행 환경의 `.git/FETCH_HEAD` 쓰기 제한으로 실행하지 못했다.
- 강제 옵션 없이 `git push origin main`을 실행했고 `c5e3939..0d96064 main -> main`으로 원격 반영에 성공했다.

## 2026-07-16 최종 목적 재점검

### 재확인 결과

- 작업 시작 시 작업 트리는 깨끗했고 `HEAD`와 `origin/main`은 모두 `22c5c96`으로 일치했다.
- 사용자 대상 Markdown 18개 문서에는 취업 준비 5단계와 기능·문서·트러블슈팅·면접 증거 ID 체계가 유지되고 있다.
- `history.html`의 타임라인은 8개 커밋만 연결하고 있어 이후 생성된 `0d96064`, `22c5c96`이 누락됐다.

### 수정 범위

- 기존 워크북 문서와 코드 조각은 이미 목적에 맞으므로 중복 수정하지 않는다.
- `history.html`의 요약 수치와 타임라인에 누락된 두 실제 커밋을 추가한다.
- 이번 이력 동기화 자체의 관리 커밋은 히스토리 유지 원칙에 따라 별도 프로그램 단계로 세지 않는다.

### 검증 기준

- Markdown 18개 문서의 상대 링크와 표 열 수가 정상이다.
- `history.html`에 연결한 10개 커밋 객체가 실제로 존재한다.
- HTML 로컬 링크, 중복 ID, 태그 중첩, CSS 괄호가 정상이다.
- `git diff --check`가 통과하고 원격 `main`에 반영된다.

### 수정과 검증 결과

- `history.html`에 `0d96064` 히스토리 확장과 `22c5c96` 최종 배포 커밋을 추가했다.
- 요약 카드의 Git 단계 수를 8개에서 10개로 실제 타임라인과 맞췄다.
- 사용자 대상 워크북 문서 15개가 취업·포트폴리오 목적과 완료·검증 기준을 포함하는지 확인했다.
- Markdown 18개 파일의 상대 링크와 표 열 수 검사 통과.
- `history.html`의 로컬 링크 9개, 실제 Git 커밋 링크 10개, 중복 ID, 태그 중첩, CSS 중괄호 검사 통과.
- `git diff --check` 통과.

### 배포 결과

- `587e6a4 docs: 최신 워크북 이력 동기화` 커밋을 생성했다.
- `git push origin main`이 성공해 `22c5c96..587e6a4 main -> main`으로 반영됐다.
- 이력 동기화와 체크리스트 갱신 같은 관리 커밋은 `history.html`의 프로그램 주요 단계 수에 포함하지 않는다.

## 2026-07-16 코드와 문서 전수 감사

### 감사 범위

- Git 추적 파일은 총 90개다.
- 코드·설정 조각은 Starter 35개와 Hard 35개로 총 70개다.
- Markdown 문서는 사용자용 15개와 계획·체크리스트·컨텍스트 노트 3개로 총 18개다.
- 정적 히스토리 문서는 `history.html` 1개다.

### 확인 기준

- Starter와 Hard가 같은 코드 챕터 경로를 제공한다.
- 각 코드 조각이 실제 구현 위치, 학습 목표, TODO 또는 빈칸을 안내한다.
- 코드 안의 문서 참조가 현재 저장소에 실제로 존재한다.
- enum, API 경로, ErrorCode, 역할 이름이 PRD/TRD와 충돌하지 않는다.
- 코드 조각을 완성 애플리케이션으로 오해하지 않고 실행 프로젝트·테스트·증거 기록으로 이어지게 한다.

### 초기 발견

- Hard 코드의 일부 주석에 현재 저장소에 없는 `spec-index` 참조가 남아 있다.
- Starter 통합 테스트 주석에 현재 저장소와 무관한 `bank-system` 참조가 남아 있다.
- 이전 문서 감사는 Markdown 중심이었으므로 이번에는 70개 코드·설정 조각을 별도 대상으로 전수 검사한다.

### 코드와 문서 정합성 결정

- 1차 인증 구현은 세션 방식으로 고정하고, JWT는 3차 확장 학습으로만 유지한다.
- `AuthService`는 이메일 정규화, 사용자 조회, 비밀번호 검증, 응답 생성만 담당한다.
- `AuthController`는 `HttpServletRequest`를 통해 세션을 생성하고 ID를 재발급한 뒤 사용자 ID와 Role을 저장하며, 로그아웃 시 기존 세션을 폐기한다.
- 1차 통합 테스트는 로그인 응답의 토큰을 파싱하지 않고 `MockHttpSession`을 다음 요청에 재사용한다.
- BCrypt만 필요한 1차 단계에는 `spring-security-crypto`, 필터 체인과 메서드 인가가 필요한 2차 단계에는 `spring-boot-starter-security`를 추가하도록 구분한다.
- 코드 조각의 학습용 빈칸은 유지하고, 존재하지 않는 `spec-index`, `bank-system` 참조만 현재 PRD/TRD와 세션 흐름으로 교체한다.

### 수정 결과

- Starter·Hard의 빌드 설정, 인증 Controller·Service, 통합 테스트를 같은 단계형 보안 흐름으로 맞췄다.
- 문제집, 정답, 기능 구현 워크북, 보안 흐름, 포트폴리오 문서화 워크북, PRD/TRD의 인증 설명을 코드와 일치시켰다.
- 부서 Service의 ErrorCode 안내와 Hard 코드의 오래된 문서 참조를 현재 공통 에러 모델과 루트 PRD/TRD 기준으로 바꿨다.

### 전수 검사 결과

- 추적 파일 90개, Markdown 18개, HTML 1개, Java 조각 66개, 설정 조각 4개를 확인했다.
- Starter 35개와 Hard 35개의 코드·설정 경로 대응 검사가 통과했다.
- 코드·설정 조각 70개의 실제 위치 안내, 학습 목표, TODO·빈칸 검사가 통과했다.
- Java 조각 66개의 중괄호 균형, 학습 질문, Hard 자가 채점 검사가 통과했다.
- 문제·정답·목차의 00~39 챕터와 Hard 코드 챕터 33개 범위 검사가 통과했다.
- 사용자 문서 15개의 취업·워크북 목적과 완료·증거 안내 검사가 통과했다.
- Markdown 18개의 상대 링크, 인라인 저장소 경로, 표 헤더 구조 검사가 통과했다.
- 이 저장소의 Java와 설정 파일은 import와 패키지를 생략한 학습용 조각이므로 Gradle 컴파일 대상이 아니며, 구조·참조·정합성 정적 검사로 검증한다.

### 전수 감사 히스토리

- `4a818c7 fix: 세션 인증 워크북 정합성 보완` 커밋을 코드·문서 전수 감사의 실제 프로그램 단계로 기록했다.
- `history.html`의 Git 단계 수를 10개에서 11개로 갱신하고 최신 항목에 코드·설정 70개와 Markdown 18개 검사 결과를 요약했다.
- `history.html`의 로컬 링크 9개, 실제 Git 커밋 링크 11개, 중복 ID, 타임라인 수, HTML 태그 중첩, CSS 중괄호 검사가 통과했다.
- 현재 세션에는 브라우저 렌더링 제어 수단이 제공되지 않아 화면 캡처 대신 반응형 CSS와 HTML 정적 구조를 검증했다.

### 전수 감사 배포 결과

- `4a818c7 fix: 세션 인증 워크북 정합성 보완`과 `1a44179 docs: 코드 전수 감사 이력 추가`를 생성했다.
- 강제 옵션 없이 `git push origin main`을 실행했고 `d37fb7c..1a44179 main -> main`으로 원격 반영에 성공했다.
- 이력 문서 자체의 동기화와 배포 기록 커밋은 `history.html`의 주요 프로그램 단계 수에 포함하지 않는다.

## 2026-07-17 HTML 학습 목차 구축

### 사용자 요청

- 취업을 위한 워크북 프로그램이라는 목적에 맞춰 초보자도 쉽게 따라갈 수 있게 수정한다.
- 목차처럼 HTML로 학습 문서를 연결한다.
- 실제 변경 이력을 `history.html`에 작성하고 Git 원격 저장소에 푸시한다.

### 확인한 현재 상태

- 작업 시작 시 `main`, `origin/main`, `HEAD`는 모두 `c4a518f`로 일치하고 작업 트리는 깨끗했다.
- 루트 README와 Markdown 워크북은 취업 준비 5단계와 40개 챕터를 안내하지만, 브라우저에서 전체 학습 동선을 한눈에 고르는 HTML 시작 화면은 없다.
- 유일한 HTML인 `history.html`은 변경 이력과 주요 문서 지도 역할을 하며, 학습 순서나 40개 챕터별 진입점은 제공하지 않는다.

### 설계 결정

- 기존 Markdown 내용을 복제하거나 기술 내용을 바꾸지 않고, 루트 `index.html`을 정적 학습 내비게이터로 추가한다.
- 외부 라이브러리와 빌드 도구 없이 파일을 직접 열어 사용할 수 있게 한다.
- 처음 시작하는 사람을 위한 5단계 흐름과 60분 학습 루프를 먼저 보여주고, 전체 40개 챕터는 주제별로 접어서 탐색하게 한다.
- 챕터 링크는 실제 Starter·Hard 파일로 직접 연결하고, 공통 문제·해설 문서 링크를 함께 제공한다.
- 기존 문서가 단일 진실 공급원이며 HTML은 학습 순서와 진입점만 제공한다.

### 검증 기준

- 00~39 챕터가 정확히 한 번씩 목차에 포함된다.
- HTML의 모든 로컬 링크가 실제 파일을 가리킨다.
- doctype, 언어, 제목 구조, 중복 ID, 태그 중첩, CSS 괄호가 정상이다.
- 데스크톱과 모바일에서 핵심 탐색 요소가 겹치거나 잘리지 않는다.
- `git diff --check`와 최종 작업 트리 검사가 통과한다.

### 구현 결과

- 루트 `index.html`에 프로그램 목적, 처음 시작하는 6단계, 60분 학습 세션과 챕터 완료 기준을 추가했다.
- 00~39 전체 챕터를 5개 코스로 묶고 Starter·Hard·워크시트 70개 학습 조각에 직접 연결했다.
- 루트 README와 `practice/README.md`의 시작 순서에 HTML 목차를 첫 진입점으로 추가했다.
- `history.html`의 주요 문서와 프로그램 지도에서도 HTML 목차로 이동하게 했다.
- 외부 라이브러리, JavaScript, 별도 빌드 도구는 추가하지 않았다.

### 1차 검증 결과

- `git diff --check` 통과.
- HTML 구조 파서로 `index.html`, `history.html`의 태그 중첩과 중복 ID 검사 통과.
- `index.html`의 00~39 챕터 순서와 5개 코스 검사 통과.
- `index.html`의 로컬 링크 101개와 `history.html`의 링크 22개 검사 통과.
- Markdown 18개 파일의 상대 링크 43개 검사 통과.
- CSS 중괄호와 920px·680px 반응형 분기 검사 통과.
- 로컬 HTTP 서버는 샌드박스의 포트 바인딩 제한으로 실행되지 않았고, Chrome은 보안 정책상 로컬 `file://` 접근을 거부했다.
- 브라우저 정책을 우회하지 않고 정적 구조·링크·반응형 CSS 검사로 대체했으므로 실제 데스크톱·모바일 렌더링은 남은 확인 사항이다.

### 커밋과 배포 시도 결과

- 원본 저장소에서 명시한 7개 파일만 `git add`하려 했으나 `.git/index.lock`을 만들 수 없다는 `Operation not permitted` 오류로 커밋이 차단됐다.
- `gh` 2.96.0은 설치되어 있지만 저장된 GitHub 토큰이 유효하지 않아 GitHub CLI 배포 절차를 사용할 수 없다.
- 셸에서 `git ls-remote origin refs/heads/main`을 실행했으나 `Could not resolve host: github.com` 오류로 원격 확인이 차단됐다.
- GitHub 연결 앱으로 원격 커밋을 구성하려 했으나 blob 생성 권한이 없어 `403 Resource not accessible by integration`이 반환됐다.
- 원격 ref를 변경하거나 새 커밋을 생성하지 않았으며, 로컬 변경 파일은 모두 보존되어 있다.
- `history.html`에는 이번 작업을 실제 커밋으로 표현하지 않고 `로컬 변경 · 배포 대기` 상태로 기록했다.
- 제한이 해제되면 `feat: HTML 학습 목차 추가`로 커밋한 뒤 강제 옵션 없이 `git push origin main`을 실행해야 한다.

## 2026-07-17 따라 쓰는 취업 워크북 대시보드 개편

### 사용자 요청

- 프로그램의 목적을 취업을 위한 워크북으로 명확히 한다.
- 처음 사용하는 사람도 완전히 쉽게 따라가도록 구조를 바꿀 수 있다.
- 실제 변경 내용을 `history.html`에 기록하고 Git 원격 저장소에 푸시한다.

### 확인한 현재 상태

- 작업 시작 시 `main`, `origin/main`, `HEAD`는 모두 `51a823b`로 일치하고 작업 트리는 깨끗했다.
- 기존 `index.html`은 프로그램 목적, 6단계 시작 순서, 60분 학습 루프와 00~39 챕터 링크를 제공한다.
- 화면은 자료 탐색에는 충분하지만 사용자가 목표와 오늘 할 일을 직접 작성하거나 완료 상태를 확인할 수 없어 워크북보다는 목차에 가깝다.
- `history.html`의 최신 항목은 이미 커밋된 `51a823b`를 아직 로컬 변경·배포 대기로 표시해 실제 Git 상태와 일치하지 않는다.

### 설계 결정

- 기존 기술 문서와 40개 챕터는 유지하고 `index.html`의 학습 순서와 상호작용만 보완한다.
- 첫 화면은 목표 설정, 현재 진척도, 바로 이어서 할 한 가지 행동을 우선 보여준다.
- 목표 직무, 마감일, 하루 학습 시간, 오늘의 FR ID·챕터와 완료 체크는 현재 브라우저의 `localStorage`에만 저장한다.
- 준비·설계·구현·검증·지원의 5단계는 각 단계의 행동, 산출물, 통과 기준을 한 카드에서 확인하고 완료 체크할 수 있게 한다.
- 00~39 챕터에는 완료 체크를 연결하되 기존 Starter·Hard·워크시트 링크와 학습 내용을 변경하지 않는다.
- 서버와 로그인 없이 동작하는 정적 워크북을 유지하고 외부 라이브러리를 추가하지 않는다.

### 검증 기준

- 목표와 오늘 학습 입력, 단계·챕터 완료 상태가 새로고침 뒤 복원된다.
- 00~39 챕터가 정확히 한 번씩 존재하고 모든 로컬 링크가 실제 파일을 가리킨다.
- HTML 태그 중첩, 중복 ID, CSS 괄호와 JavaScript 구문이 정상이다.
- 데스크톱과 모바일에서 목표 설정, 다음 행동, 진행률, 챕터 링크가 겹치거나 잘리지 않는다.
- `git diff --check`가 통과하고 변경사항이 의미 단위로 커밋되어 `origin/main`에 반영된다.

### 구현 결과

- 첫 화면에 5단계와 40개 챕터를 합산한 전체 진행률, 단계·챕터 완료 수, 지원 마감 D-Day와 다음 행동을 표시하는 대시보드를 추가했다.
- 목표 직무, 첫 지원 마감일, 하루 학습 시간을 작성하는 시작 설정을 추가했다.
- 준비·설계·구현·검증·지원 카드마다 행동, 통과 기준, 관련 문서와 완료 체크를 연결했다.
- 오늘의 FR ID, 챕터, 완료 조건과 60분 학습 순서 체크를 한 화면에서 작성하게 했다.
- 00~39 각 챕터에 완료 버튼을 추가하고 전체 펼치기·접기 기능을 제공했다.
- 모든 입력과 체크 상태를 `company-workbook-state-v1` 키로 현재 브라우저에 저장하고 다시 열 때 복원하도록 했다.
- 루트 README와 학습 안내 문서를 새 HTML 취업 워크북 흐름과 브라우저 저장 범위에 맞게 수정했다.

### 1차 검증 결과

- `git diff --check` 통과.
- `index.html`의 JavaScript를 추출한 `node --check` 통과.
- `index.html`, `history.html`의 HTML 태그 중첩, 중복 ID와 로컬 링크 검사 통과.
- `index.html`의 00~39 챕터 40개, 취업 준비 단계 5개, 오늘 학습 체크 5개 범위 검사 통과.
- Markdown 문서의 상대 링크 40개와 표 행 772개 검사 통과.
- 인앱 브라우저 런타임에는 사용 가능한 브라우저 인스턴스가 없어 실제 데스크톱·모바일 렌더링과 새로고침 복원을 실행하지 못했다.
- 로컬 HTTP 서버도 실행 환경의 포트 바인딩 권한 제한으로 시작하지 못해 정적 구조·구문·링크 검사로 대체했다.

### 초기 히스토리 갱신과 배포 차단 상태

- 이전 HTML 목차 작업이 실제 `51a823b feat: HTML 학습 목차 추가`로 커밋되어 `origin/main`에 반영됐음을 GitHub 연결 앱에서 확인했다.
- `history.html`의 이전 배포 대기 항목을 실제 커밋 링크로 바꾸고 Git 단계 수를 12개로 맞췄다.
- 이번 대시보드 개편은 실제 커밋 전이므로 새 최신 항목에 로컬 변경·배포 대기로 구분해 기록했다.
- 명시한 7개 변경 파일을 `git add`하려 했으나 `.git/index.lock`을 만들 수 없다는 `Operation not permitted` 오류로 로컬 커밋이 차단됐다.
- `github:yeet` 스킬의 필수 확인 결과 `gh` 2.96.0은 설치되어 있지만 저장된 `seungikyang` 계정 토큰이 유효하지 않다.
- GitHub 연결 앱은 저장소와 `51a823b` 읽기에 성공했지만 실제 변경 blob 생성은 `403 Resource not accessible by integration`으로 거부됐다.
- 로컬 변경 파일은 모두 보존되어 있으며 원격 ref는 변경하지 않았다.

### 재개 검증 결과

- 사용자가 `/usr/local/bin/gh`의 `seungikyang` 계정 로그인을 완료하고 `repo`, `workflow` 범위가 있는 인증 상태를 확인했다.
- `index.html`의 JavaScript를 추출한 `node --check`가 다시 통과했다.
- `index.html`, `history.html`의 HTML 태그 중첩, 중복 ID와 로컬 링크 검사가 다시 통과했다.
- 00~39 챕터 40개, 취업 준비 단계 5개, 오늘 학습 체크 5개와 JavaScript 대상 ID 검사가 통과했다.
- Markdown 상대 링크 40개, `history.html`의 실제 Git 커밋 객체 12개와 `git diff --check` 검사가 통과했다.
- 브라우저 런타임의 사용 가능한 인스턴스 목록이 비어 있어 실제 렌더링과 상호작용 검증은 남은 수동 확인 사항으로 유지한다.
- README, 계획·체크리스트·컨텍스트 노트, `index.html`, `history.html`, 학습 안내까지 변경 파일 7개를 요청 범위와 대조해 전수 검토했다.

## 2026-07-17 임시 작업 회수와 루트 작업 고정

### 임시 작업 확인 결과

- Git에 등록된 worktree는 `/Users/seungik.yang/Documents/company-work-system` 하나뿐이다.
- `/private/tmp/company-work-system-publish.n8Xyk1`에서 로컬 커밋 `43e4dc9 feat: 따라 쓰는 취업 워크북 대시보드 개편`을 찾았다.
- 임시 저장소의 일반 작업 파일은 정리되어 삭제 상태로 보이지만 Git 객체와 커밋은 남아 있다.
- 루트의 추적 파일 91개를 `43e4dc9`의 트리와 blob 해시로 비교한 결과 불일치가 0개였다.
- 따라서 임시 경로의 파일로 루트를 덮어쓸 필요가 없으며, 루트의 7개 미커밋 파일이 임시 커밋의 작업 내용을 완전하게 보존한다.

### 작업 위치 고정 결정

- 저장소 루트에 `AGENTS.md`를 두고 실제 `pwd -P`와 `git rev-parse --show-toplevel` 결과가 같은지 작업 전에 확인하게 한다.
- `/tmp`, `/private/tmp`, `$TMPDIR`, `~/.codex/worktrees`의 clone이나 worktree에서는 수정·테스트·커밋·푸시를 금지한다.
- 루트 `.git` 쓰기가 제한되면 임시 저장소로 우회하지 않고 루트 변경을 보존한 채 제약을 보고한다.
- 이번 세션에서도 파일 수정과 검증은 프로젝트 루트에서만 수행한다.

### 확인된 환경 제약

- 루트에서 임시 커밋을 가져오는 `git fetch /private/tmp/company-work-system-publish.n8Xyk1 43e4dc9...`는 `.git/FETCH_HEAD`를 열 수 없는 `Operation not permitted` 오류로 실패했다.
- 루트에서 실행한 `git ls-remote origin refs/heads/main`은 `Could not resolve host: github.com` 오류로 실패했다.
- `gh auth status`는 저장된 `seungikyang` 계정 토큰이 유효하지 않다고 보고했다.
- GitHub 연결 앱에서는 원격 커밋 `51a823b`가 존재함을 확인했으며, 임시 커밋 `43e4dc9`는 원격에 존재하지 않음을 확인했다.

### 최종 검증과 배포 차단

- 저장소 루트 확인 명령이 `/Users/seungik.yang/Documents/company-work-system`에서 통과했다.
- `index.html` JavaScript를 추출한 `node --check`가 통과했다.
- `index.html`, `history.html`의 태그 중첩, 중복 ID, CSS 괄호와 로컬 링크 107개 검사가 통과했다.
- 00~39 챕터 40개, 취업 준비 단계 5개, 세션 체크 5개와 JavaScript 대상 ID 검사가 통과했다.
- Markdown 19개 파일의 상대 링크 40개 검사와 `git diff --check`가 통과했다.
- README, `history.html`, `index.html`, `practice/README.md`는 임시 커밋 `43e4dc9`의 blob과 계속 일치한다.
- GitHub 연결 앱의 blob 생성도 `403 Resource not accessible by integration`으로 거부되어 원격 커밋 생성과 `main` 갱신을 진행할 수 없다.
- 임시 Git 저장소로 커밋·푸시를 우회하지 않았으며 루트 변경 파일을 그대로 보존했다.

### 루트 재개와 커밋 결과

- 현재 세션에서 저장소 루트의 `.git` 쓰기와 셸 실행 권한이 정상임을 확인했다.
- 관련 임시 저장소는 `/private/tmp/company-work-system-publish.n8Xyk1` 하나이며, 다른 두 임시 저장소는 `fullstack-portfolio` 원격을 사용하므로 작업 범위에서 제외했다.
- 루트에서 임시 커밋 `43e4dc9`를 읽기 전용 대체 객체로 참조해 비교한 결과 README, `history.html`, `index.html`, `practice/README.md`가 정확히 일치했다.
- 루트의 `plan.md`, `checklist.md`, `context-notes.md`에는 임시 커밋 이후 회수 판단과 루트 고정 기록 66줄이 추가되어 있었다.
- 임시 경로의 파일이나 Git 상태를 수정하지 않았고, 제품 변경은 루트 작업 파일에서 직접 `56d7eb2 feat: 따라 쓰는 취업 워크북 대시보드 개편`으로 커밋했다.
- 루트 `AGENTS.md`는 파일 수정·테스트·빌드·커밋·푸시 전에 실제 루트 경로를 확인하고 임시 clone·worktree를 사용하지 못하게 하는 저장소 지침으로 유지한다.
- `git diff --check`, 추출 JavaScript의 `node --check`, HTML 태그·중복 ID·로컬 링크 102개, 챕터 40개·단계 5개·세션 체크 5개 검사가 루트에서 통과했다.

### 최종 원격 반영 결과

- 루트에서 `git push origin main`을 강제 옵션 없이 실행해 `51a823b..41687a4` 범위를 푸시했다.
- `56d7eb2 feat: 따라 쓰는 취업 워크북 대시보드 개편`과 `41687a4 chore: 저장소 루트 작업 규칙 추가`가 원격 `main`에 반영됐다.
- 푸시 직후 로컬 `HEAD`와 `origin/main`이 모두 `41687a46e797ecb678334f4d7bbd3c88dd237c20`으로 일치함을 확인했다.

## 2026-07-19 따라가는 학습·구현 루프 고도화

### 사용자 요청과 해석

- 프로그램 목적은 Java·Spring Boot 취업 결과물을 만드는 워크북이다.
- HTML 목차처럼 모든 학습 자료를 쉽게 연결하고 공부와 프로그램 구현을 함께 따라가게 해야 한다.
- 목표를 만족할 때까지 구현과 검증을 반복하고 `history.html` 갱신, 커밋과 원격 푸시까지 마쳐야 한다.
- “완벽한 검증”은 정적 구조·링크·JavaScript·상태 계약과 실제 데스크톱·모바일 상호작용을 모두 검증하는 측정 가능한 기준으로 해석한다.

### 시작 상태

- 작업 시작 시 실제 저장소 루트는 `/Users/seungik.yang/Documents/company-work-system`이고 등록된 worktree도 이 경로 하나뿐이다.
- `main`, `origin/main`, `HEAD`는 모두 `5d9bf0b`로 일치하며 작업 트리는 깨끗하다.
- 기존 `index.html`은 목표 입력, 5단계 로드맵, 오늘 학습, 00~39 챕터와 취업 증거 기준을 제공한다.
- 오늘 챕터를 선택해도 해당 챕터의 문제·Hard·Starter로 바로 이어지는 실행 패널이 없어 사용자가 아래 40개 목록에서 다시 찾아야 한다.
- HTML·링크·JavaScript 검증은 이전 세션의 일회성 명령으로만 남아 있어 다음 변경 후 같은 기준을 재실행하기 어렵다.

### 설계 결정

- 기존 화면과 브라우저 저장 키 `company-workbook-state-v1`은 호환성을 위해 유지한다.
- 목표 설정부터 취업 증거까지 여섯 단계를 번호형 HTML 목차로 연결하고 각 단계의 완료 산출물을 함께 표시한다.
- 오늘 챕터 선택값을 실제 챕터 행의 제목·설명·링크와 연결해 문제, 직접 풀이, 힌트, 본인 프로젝트 구현, 증거 기록의 다섯 행동을 보여준다.
- 외부 패키지 없이 표준 Python과 Node.js만 사용해 검증을 한 명령으로 반복 가능하게 만든다.
- 정적 검사 후 실제 브라우저에서 데스크톱과 모바일 뷰포트, 입력, 체크, 선택 챕터 링크와 새로고침 복원을 확인한다.

### 구현 결과

- `index.html` 상단에 취업 목표, 현재 단계, 오늘 학습, 직접 풀이, 본인 프로젝트 구현, 검증·증거 기록을 연결한 번호형 6단계 목차를 추가했다.
- 오늘 챕터 선택값을 00~39 실제 챕터 행에 연결하고 제목·설명·Hard·Starter·워크시트 경로를 실행 패널에 자동 표시한다.
- 선택한 챕터를 전체 목차에서 다시 찾지 않도록 해당 코스를 펼치고 행으로 스크롤하는 버튼을 추가했다.
- Hard가 없는 문서형 챕터는 워크시트 직접 작성 뒤 정답과 해설을 확인하도록 별도 분기를 적용했다.
- `scripts/verify_workbook.py`에서 HTML 태그와 중복 ID, local fragment·파일 링크, Markdown 링크, 6단계·5단계·40개 챕터 계약, JavaScript 구문·DOM ID와 반응형 분기를 한 명령으로 검사한다.
- README의 시작 순서와 주요 문서 설명, `history.html`의 최신 대표 이력을 실제 동선에 맞게 갱신했다.

### 검증 루프 결과

- 1차 `python3 scripts/verify_workbook.py`에서 HTML 로컬 링크 125개, Markdown 상대 링크 40개와 모든 구조 계약이 통과했다.
- 데스크톱 브라우저에서 목표 직무, 60분, FR ID, 챕터 10, 완료 조건, 준비 단계·세션·챕터 완료를 입력하고 진행률 4%와 실제 Hard·Starter 링크를 확인했다.
- 새로고침 뒤 모든 입력, 체크, 선택 챕터, 진행률과 링크가 복원되고 브라우저 오류 로그가 없음을 확인했다.
- 챕터 22 분기 검사에서 직접 작성과 확인 단계가 같은 워크시트를 가리키는 중복을 발견했다.
- 확인 단계를 정답·해설로 변경하고 정적 검사와 브라우저 새로고침 검사를 다시 실행해 수정 결과를 확인했다.
- 전체 목차 보기 버튼이 선택 챕터의 코스를 펼치고 해당 행을 실제 뷰포트 안으로 이동시키는 것을 확인했다.
- 390×844 모바일 뷰포트에서 목차·로드맵·세션·선택 챕터 루프가 모두 1열이고 문서 가로 넘침과 화면 밖 컨트롤이 0개임을 확인했다.
- 모바일에서 챕터 35를 선택해 워크시트·정답 해설 연결과 상호작용을 다시 확인했다.

### 기능 커밋 결과

- README, 계획·체크리스트·컨텍스트 노트, `history.html`, `index.html`과 검증 스크립트 7개 파일을 전수 검토했다.
- 루트에서 `686efee feat: 따라가는 학습과 구현 루프 연결` 커밋을 생성했다.
- `history.html`의 최신 항목을 실제 커밋 링크 `686efeeedbf323dcb69570c10030c3e2b7ca52d8`에 연결했다.
- 검증 중 생성된 Python bytecode cache는 삭제하고 `.gitignore`에 재발 방지 패턴을 추가했다.

### 원격 반영 결과

- 루트에서 강제 옵션 없이 `git push origin main`을 실행해 `5d9bf0b..80e18b3` 범위를 푸시했다.
- 기능 커밋 `686efee`와 이력 동기화 커밋 `80e18b3`가 원격 `main`에 반영됐다.
- 푸시 직후 로컬 `HEAD`와 `origin/main`이 모두 `80e18b3b45340409a462b9cc4c2c2df38658dcb0`으로 일치함을 확인했다.

## 2026-07-19 반복 가능한 검증 폐쇄 루프

### 재감사 결과

- 작업 시작 시 실제 저장소 루트, 등록 worktree, `main`, `origin/main`, `HEAD`가 모두 정상이고 작업 트리는 깨끗했다.
- `python3 scripts/verify_workbook.py`는 HTML 링크 125개, Markdown 링크 40개와 기존 학습 계약을 모두 통과했다.
- 번호형 6단계 목차와 선택 챕터의 문제·Hard·Starter·워크시트 연결은 이미 구현되어 있어 중복 추가하지 않는다.
- 오늘 학습은 FR ID, 챕터, 완료 조건과 5개 세션 체크를 저장하지만 실제 테스트 명령과 실패·통과 결과는 기록하지 않는다.
- 다음 학습을 시작할 때 오늘 필드와 세션 체크를 하나씩 비워야 하므로 반복 루프의 종료와 재시작 경계가 분명하지 않다.

### 설계 결정

- 기존 저장 키 `company-workbook-state-v1`과 상태 병합 방식을 유지해 이전 브라우저 데이터와 호환한다.
- 오늘 상태에 `testCommand`와 `testResult`만 추가하고 서버나 외부 라이브러리는 도입하지 않는다.
- 검증 결과는 미실행·실패·통과 세 상태로 제한해 사용자가 다음 행동을 바로 이해하게 한다.
- 새 학습 시작은 `today`와 `session`만 초기값으로 되돌리고 `profile`, `stages`, `chapters`는 보존한다.
- 브라우저에서 실패 안내, 통과 안내, 새로고침 복원과 선택적 초기화 경계를 직접 검증한다.

### 구현 결과

- 오늘 학습에 실행할 검증 명령과 미실행·실패·통과 결과 선택을 추가했다.
- 기존 상태 병합을 통해 이전 저장 데이터에는 새 필드의 빈 기본값을 적용한다.
- 명령 미입력, 실행 전, 실패, 통과 상태마다 다음 행동을 별도 상태 패널에 표시한다.
- 실패 상태는 오류 메시지·실패 입력 기록과 최소 수정 후 같은 명령 재실행을 안내한다.
- 통과 상태는 회귀 테스트, 커밋, 배운 점과 `E-{FR ID}` 증거 연결을 안내한다.
- 새 학습 시작 버튼은 오늘 필드와 세션 체크만 초기화하고 목표·단계·완료 챕터는 유지한다.
- 검증 스크립트에 새 필수 ID, 실패·통과 선택지와 렌더링·초기화 함수 계약을 추가했다.
- README에 오늘 학습 검증 루프와 초기화 경계를 설명하고 `history.html`에 최신 대표 이력을 추가했다.

### 브라우저 검증 결과

- 기존 브라우저 상태를 통해 목표 직무, 준비 단계, 완료 챕터 1개와 오늘 학습이 이전 저장 형식에서 정상 로드됨을 확인했다.
- `./gradlew test`와 실패 결과를 입력하자 오류 기록·최소 수정·같은 명령 재실행 안내가 표시됐다.
- 통과로 변경하자 회귀 테스트·커밋·증거 ID 연결 안내로 전환됐다.
- 새로고침 뒤 검증 명령, 통과 결과, 상태 안내와 기존 진척도가 모두 복원되고 브라우저 오류 로그가 없었다.
- 새 학습 시작 후 FR ID·챕터·완료 조건·검증 명령·결과와 세션 체크만 비워졌다.
- 같은 시점에 목표 직무, 준비 단계, 완료 챕터 1개와 전체 진행률 4%는 유지됐다.
- 390×844 모바일에서 세션은 1열, 동작 버튼은 세로 배치되며 보이는 세션 컨트롤 16개와 문서에 가로 넘침이 없었다.

### 최종 검증 결과

- `python3 scripts/verify_workbook.py`가 HTML 로컬 링크 125개, Markdown 상대 링크 40개와 전체 학습·DOM·JavaScript 계약을 통과했다.
- `git diff --check`가 통과했다.
- `history.html`은 최신 날짜 2026-07-19, 대표 이력 15개와 최신 제목을 브라우저에서 확인했다.
- 모바일 `history.html`에 가로 넘침과 브라우저 오류 로그가 없었다.
- README, 계획·체크리스트·컨텍스트 노트, `history.html`, `index.html`, 검증 스크립트 7개 파일을 요청 범위와 대조해 전수 검토했다.

### 기능 커밋 결과

- 루트에서 `0ed815c feat: 반복 가능한 검증 폐쇄 루프 추가` 커밋을 생성했다.
- `history.html`의 최신 대표 이력을 실제 커밋 `0ed815ca76fc4d923de2d4e58a37ea9d770c5fe7`에 연결했다.

### 원격 반영 결과

- 루트에서 강제 옵션 없이 `git push origin main`을 실행해 `2a4f8e4..d3794e7` 범위를 푸시했다.
- 기능 커밋 `0ed815c`와 이력 동기화 커밋 `d3794e7`이 원격 `main`에 반영됐다.
- 푸시 직후 로컬 `HEAD`와 `origin/main`이 모두 `d3794e793ce5c8799d0b986df9230e7286ccbf59`로 일치함을 확인했다.

## 2026-07-24 폴더별 취업 결과물 계약과 전수 검증

### 시작 상태와 전수 감사

- 실제 저장소 루트는 `/Users/seungik.yang/Documents/company-work-system`이고 작업 시작 시 `main`, `origin/main`, `HEAD`는 `f84e370`으로 일치하며 작업 트리는 깨끗했다.
- `.openai/hosting.json`은 없어 기존 정적 HTML 저장소 방식으로 작업한다.
- `practice/starter`에는 00~39의 40개 폴더와 학습 파일 42개가 있다.
- `practice/hard`에는 코드형 챕터 00~19, 21, 23~34의 33개 폴더와 학습 파일 35개가 있다.
- 문서형 챕터 20, 22, 35~39는 직접 서술하는 워크시트이므로 기존 설계대로 Hard를 별도 제공하지 않는다.
- 프로그램 파일 77개에는 빈 파일이 없고 모든 Java·설정 조각에 TODO 또는 빈칸 표식이 있다.
- Hard의 모든 학습 파일은 같은 상대 경로의 Starter 파일과 대응한다.
- 기존 `python3 scripts/verify_workbook.py`는 HTML 링크 125개, Markdown 링크 40개와 40개 챕터 계약을 통과했다.

### 확인된 검증 공백과 설계 결정

- 기존 검사는 챕터마다 학습 링크가 하나 이상 있는지만 확인해 폴더 안의 모든 프로그램 파일이 HTML에 연결됐는지는 보장하지 않는다.
- 챕터 설명은 학습 주제를 말하지만 취업용 결과물과 실제 완료 검증은 전체 공통 안내에서만 설명해 선택한 폴더의 종료 조건을 다시 찾아야 한다.
- 각 챕터 행에 `data-career-output`과 `data-career-check` 계약을 두고 선택 챕터 패널에서 결과물과 검증을 함께 보여준다.
- 정적 검증은 73개 폴더의 번호·이름, 77개 파일의 비어 있지 않음·워크북 표식·HTML 연결, Hard와 Starter의 파일 대응을 검사한다.
- 학습 조각의 TODO와 빈칸은 사용자가 직접 작성해야 하므로 정답을 채우거나 실행 애플리케이션으로 합치지 않는다.

### 구현과 검증 루프 결과

- 00~39 챕터 행마다 해당 프로그램으로 만들 취업 결과물과 구체적인 완료 검증을 연결했다.
- 선택 챕터 패널에 결과물·검증 계약을 2열로 표시하고 680px 이하에서는 1열로 전환한다.
- 코드형 챕터는 Hard와 Starter를, 문서형 챕터는 워크시트와 정답·해설을 기존 규칙대로 연결한다.
- 검증 스크립트는 Starter 40개·Hard 33개 폴더 번호, 파일 77개의 형식·첫 줄 역할·TODO 또는 빈칸·HTML 연결을 전수 검사한다.
- Hard 35개 학습 파일은 같은 상대 경로의 Starter 파일이 있는지 자동 검사한다.
- 1차 `python3 scripts/verify_workbook.py`와 `git diff --check`가 통과했다.
- 브라우저에서 00~39 챕터를 하나씩 선택해 제목·취업 결과물·완료 검증이 모두 표시되는지 확인했고 실패는 0개였다.
- 챕터 21은 Hard·Starter 테스트 파일로, 챕터 38은 워크시트·정답 해설로 정확히 분기됐다.
- 1280×900에서 결과물 계약은 2열이고 문서 가로 넘침이 없었다.
- 390×844에서 결과물 계약은 1열이고 오늘 학습 컨트롤 17개 중 화면 밖으로 넘친 항목이 0개였다.
- 브라우저 검증 뒤 오늘 학습만 초기화해 선택값과 결과물 계약이 초기 상태로 돌아오는 것을 확인했다.

### 기능 커밋과 히스토리 동기화

- 루트에서 `56b3703 feat: 폴더별 취업 결과물 계약 연결` 커밋을 생성했다.
- `history.html`의 대표 이력을 16개로 늘리고 최신 항목을 실제 기능 커밋 `56b3703d3524cba20a67e6e3e35585efd2dfd5ae`에 연결했다.
- 최신 이력에는 73개 프로그램 폴더·77개 학습 파일 전수 연결과 데스크톱·모바일 검증 결과를 기록했다.
- 히스토리 브라우저 점검에서 마지막 문서 갱신일이 2026-07-17로 남은 것을 발견해 최신 이력과 같은 2026-07-24로 바로잡았다.
- 검증 스크립트에 대표 이력·커밋 링크·날짜·요약 수와 마지막 갱신일의 상호 일치 검사를 추가했다.
- 390×844 모바일 히스토리는 실제 문서 너비와 스크롤 너비가 모두 375px이고 화면 밖 카드가 0개였다.

## 2026-07-24 연속 학습 탐색과 파일 내부 계약

### 시작 상태와 재감사

- 실제 저장소 루트에서 작업을 시작했고 작업 트리는 깨끗했다.
- 로컬 `main`은 이전 요청의 기능·이력 커밋 2개만큼 `origin/main`보다 앞서 있다.
- `python3 scripts/verify_workbook.py`는 프로그램 폴더 73개·학습 파일 77개·대표 이력 16개 계약을 통과했다.
- 코드·설정 조각 70개는 모두 첫 부분에 `목표` 헤더가 있다.
- 서술형 워크시트 7개는 본문에 목적과 완료 증거가 있으나 같은 위치에서 확인할 공통 챕터 계약 형식은 없다.
- HTML 선택 패널은 취업 결과물과 완료 검증을 보여주지만 연속된 챕터를 이전·다음으로 이동하는 탐색이 없다.

### 설계 결정

- 이미 목적이 있는 코드·설정 조각 70개를 기계적으로 반복 수정하지 않는다.
- 서술형 7개에는 상단에 학습 목표·취업 결과물·완료 검증 표를 추가해 파일을 직접 열어도 종료 조건을 알게 한다.
- 선택 패널에는 현재 위치와 이전·다음 버튼만 추가하고 기존 드롭다운·저장 키·5단계 학습 루프는 유지한다.
- 정적 검증은 모든 코드·설정의 목표 헤더와 모든 Markdown 프로그램의 챕터 계약을 검사한다.
- 각 프로그램 링크의 폴더 번호가 해당 HTML 챕터 번호와 같은지도 검사한다.

### 구현과 검증 루프 결과

- 서술형 워크시트 7개의 첫 설명 아래에 학습 목표·취업 결과물·완료 검증 챕터 계약을 추가했다.
- HTML 선택 패널에 현재 위치, 이전 챕터와 다음 챕터 버튼을 추가했다.
- 00에서는 이전 버튼, 39에서는 다음 버튼을 비활성화하고 중간 챕터에서는 양방향 이동한다.
- 선택 변경은 기존 오늘 챕터 상태에 저장되며 새 학습 시작은 연속 탐색 패널도 초기 상태로 되돌린다.
- 검증 스크립트는 코드·설정 70개의 목표 헤더, Markdown 7개의 챕터 계약과 세 항목을 전수 검사한다.
- 각 HTML 챕터 안의 Starter·Hard·워크시트 링크가 같은 두 자리 번호의 폴더를 가리키는지 검사한다.
- `python3 scripts/verify_workbook.py`와 `git diff --check` 1차 루프가 통과했다.
- 브라우저에서 00→39 정방향 39회와 39→00 역방향 39회를 실제 버튼으로 이동했고 실패는 각각 0개였다.
- 첫 위치는 `1 / 40`, 마지막 위치는 `40 / 40`으로 표시되고 양 끝 비활성화 상태가 정확했다.
- 1280×900에서 탐색은 가로형이고 문서 너비와 스크롤 너비가 1265px로 같았다.
- 390×844에서 탐색은 세로형이고 문서 너비와 스크롤 너비가 375px로 같으며 화면 밖 컨트롤은 0개였다.
- 브라우저 검증 뒤 새 학습 시작으로 오늘 챕터와 연속 탐색 표시를 초기 상태로 되돌렸다.

### 기능 커밋과 히스토리 동기화

- 루트에서 `e70947d feat: 연속 챕터 학습 탐색 추가` 커밋을 생성했다.
- `history.html`의 대표 이력을 17개로 늘리고 최신 항목을 실제 기능 커밋 `e70947d151f4b827446e6274eb10aa7797b56196`에 연결했다.
- 최신 이력에는 파일 77개의 내부 계약, 번호별 폴더 대응, 정방향·역방향 브라우저 전수 탐색과 반응형 검증 결과를 기록했다.
- 모바일 브라우저에서 최신 제목·커밋·대표 이력 17개 설명을 확인했고 문서 너비와 스크롤 너비가 375px로 같으며 화면 밖 카드가 0개였다.

## 2026-07-24 임시 작업 감사·루트 가드·원격 반영

### 시작 상태

- 실제 저장소 루트와 물리 `pwd`는 모두 `/Users/seungik.yang/Documents/company-work-system`이다.
- 등록된 Git worktree는 이 실제 루트 하나뿐이다.
- 로컬 `main`은 `origin/main`보다 4커밋 앞서 있고 뒤처진 커밋은 0개다.
- 작업 시작 시 추적·미추적 변경이 없는 깨끗한 작업 트리였다.

### 임시 경로 감사 결과

- `/tmp`, `/private/tmp`, 현재 `$TMPDIR`, `~/.codex/worktrees`에서 관련 저장소 이름, `.git` 메타데이터와 `company-work-system` 원격 문자열을 검색했다.
- `~/.codex/worktrees` 디렉터리는 현재 존재하지 않는다.
- 이전에 확인했던 `/private/tmp/company-work-system-publish.n8Xyk1`도 현재 존재하지 않는다.
- 이 저장소와 관련된 임시 clone·worktree·일반 파일은 0개다.
- 따라서 루트로 회수할 커밋·미커밋·미추적 작업과 삭제할 관련 임시 경로도 0개다.

### 루트 강제 보강 결정

- 기존 `AGENTS.md`의 실제 루트 확인과 임시 작업 금지 규칙은 유지한다.
- 사람과 에이전트가 같은 검사를 재사용하도록 `scripts/assert_repo_root.sh`를 추가한다.
- 가드는 현재 물리 경로가 Git 최상위와 같은지 확인하고 `/tmp`, `/private/tmp`, `$TMPDIR`, `~/.codex/worktrees` 아래의 실행을 거부한다.
- 모든 파일 수정·테스트·빌드·커밋·푸시 전에 루트에서 가드를 실행하도록 `AGENTS.md`의 시작 게이트를 강화한다.

### 루트 가드와 삭제 결과

- `scripts/assert_repo_root.sh`를 실행 가능 파일로 추가했다.
- 실제 루트에서 가드를 실행해 `/Users/seungik.yang/Documents/company-work-system` 확인 메시지와 종료 코드 0을 확인했다.
- `sh -n scripts/assert_repo_root.sh`가 통과했다.
- `python3 scripts/verify_workbook.py`와 `git diff --check`가 통과했다.
- 관련 임시 clone·worktree·파일은 감사 시점에 0개였으므로 삭제할 대상도 0개였다.
- 무관한 임시 파일이나 다른 저장소는 삭제하지 않았다.

### 커밋과 원격 반영 결과

- 루트에서 `f13f92e chore: 저장소 루트 작업 가드 추가` 커밋을 생성했다.
- `git fetch origin main` 후 로컬이 5커밋 앞서고 뒤처짐 0개임을 확인했다.
- 강제 옵션 없이 `git push origin main`을 실행해 `f84e370..f13f92e` 범위를 원격에 반영했다.
- 푸시 후 다시 원격을 가져와 로컬 `HEAD`와 `origin/main`이 모두 `f13f92ea4ff067e12c333dbcd4a1b5e74d49312f`로 일치함을 확인했다.

## 2026-07-26 챕터별 문제집 상호작용과 학습 검증

### 시작 상태와 감사 결과

- 실제 저장소 루트와 물리 `pwd`는 `/Users/seungik.yang/Documents/company-work-system`으로 일치하고 루트 가드가 통과했다.
- 작업 시작 시 `main`, `origin/main`, `HEAD`는 `a0d219a`로 일치하며 작업 트리는 깨끗했다.
- 등록된 Git worktree는 실제 저장소 루트 하나뿐이고 1차 임시 경로 검색에서 관련 clone·worktree는 발견되지 않았다.
- `python3 scripts/verify_workbook.py`는 HTML 2개, 로컬 링크 125개, Markdown 링크 40개, 프로그램 폴더 73개와 학습 파일 77개 계약을 통과했다.
- 루트 HTML은 취업 목표, 6단계 학습 목차, 5단계 로드맵, 40개 챕터, 이전·다음 탐색과 테스트 결과 기록을 이미 제공한다.
- 챕터별 문제와 해설은 별도 Markdown 문서에 있지만 선택 챕터 안에서 답을 쓰고 힌트·정답을 개별 확인하는 문제집 상호작용은 없다.

### 설계 결정

- 기존 정적 HTML, 저장 키 `company-workbook-state-v1`, 프레임워크 없는 구조를 유지한다.
- 문제는 선택 챕터의 제목과 설명에서, 힌트는 취업 결과물 계약에서, 정답 기준은 완료 검증 계약에서 구성해 40개 챕터가 같은 규칙으로 누락 없이 동작하게 한다.
- 정답을 복사하는 기능 대신 핵심 책임·실패 조건·검증 방법·면접 설명을 본인 말로 작성하고 기준과 비교하는 회상 문제를 제공한다.
- 답안과 비교 완료 기록은 오늘 세션보다 긴 학습 자산이므로 새 학습 시작에서 지우지 않는다.
- 상세 기술 해설은 기존 `practice/answers.md`를 단일 출처로 유지하고 HTML에는 중복된 장문 정답 대신 비교 기준과 해설 링크를 제공한다.
- 문제집 진행 수를 별도 지표로 보여주되 기존 5단계·40개 완료율 계산은 바꾸지 않아 이전 사용자 진척도와 의미를 유지한다.
- 정적 계약 검사와 실제 브라우저 상호작용 검사를 모두 통과할 때까지 같은 검증을 반복한다.

### 구현 결과

- 선택 챕터 패널에 취업형 회상 문제, 챕터별 답안 입력, 1단계 힌트, 정답 비교 기준과 비교 완료 체크를 추가했다.
- 문제는 챕터 제목, 힌트는 챕터 설명과 취업 결과물, 정답 기준은 핵심 책임·구현 결과물·완료 검증 계약에서 구성한다.
- 기존 저장 상태에 `workbook.answers`와 `workbook.reviewed`를 호환 방식으로 추가해 이전 브라우저 데이터가 없어도 기본값으로 동작한다.
- 답안을 쓰기 전에는 비교 완료 체크를 비활성화하고, 답안을 지우면 비교 완료도 함께 해제한다.
- 대시보드에 40개 챕터의 문제 비교 완료 수를 별도 표시하고 기존 전체 완료율 계산은 유지했다.
- 오늘 학습 초기화는 오늘 필드와 세션 체크만 비우며 목표·완료 진척도·챕터별 문제집 기록은 보존한다.
- 검증 스크립트에 문제집 DOM ID, 저장 상태, 렌더링·토글 함수, CSS 숨김 계약을 추가했다.
- README, 학습 안내, 문제와 해설 문서에 `답안 작성 → 힌트 → 직접 풀이 → 정답 비교 → 구현·검증` 순서를 반영했다.

### 반복 검증 결과

- `python3 scripts/verify_workbook.py`가 HTML 로컬 링크 126개, Markdown 상대 링크 41개, 프로그램 폴더 73개·학습 파일 77개와 새 문제집 계약을 통과했다.
- `git diff --check`가 통과했다.
- 실제 브라우저에서 챕터 10의 문제·힌트·핵심 책임·결과물·검증 기준이 휴가 신청 계약과 일치함을 확인했다.
- 답안 작성 전 비교 체크 비활성화, 답안 작성 중 상태, 정답 비교 완료 상태와 대시보드 `1 / 40` 갱신을 확인했다.
- 새로고침 뒤 선택 챕터·답안·비교 완료·진행 수가 복원되고 힌트·정답 패널은 닫힌 초기 상태로 돌아오는 것을 확인했다.
- 챕터 11은 빈 답안으로 분리되고 다시 10으로 이동하면 기존 답안이 복원되는 챕터별 저장 경계를 확인했다.
- 오늘 학습 초기화 뒤 FR ID·챕터·세션 체크는 비워지고 문제집 완료 수는 유지되는 장기 기록 경계를 확인했다.
- 00~39를 실제 선택 컨트롤로 전수 이동해 문제 제목·질문·힌트·세 가지 정답 기준·답안 접근성 이름이 모두 해당 번호와 연결되며 누락 0개임을 확인했다.
- 1280×900에서 대시보드 지표는 1행, 문제집 답안 너비는 1008px이고 문서 가로 넘침과 화면 밖 도구가 0개였다.
- 390×844에서 문서 너비와 스크롤 너비는 375px로 같고 문제집의 버튼·링크·답안·체크 중 화면 밖 컨트롤은 0개였다.
- 브라우저 오류 로그는 0개였다.
- 1차 브라우저 확인에서 취업 결과물 뒤 조사가 어색한 힌트 문장을 발견해 조사 없는 두 문장으로 수정하고 다시 확인했다.

### 기능 커밋과 이력 연결

- 루트에서 `04c462b feat: 챕터별 취업 문제집 추가` 커밋을 생성했다.
- `history.html`의 대표 이력을 18개로 늘리고 최신 항목을 실제 기능 커밋 `04c462b7719198e1dbbd733d83f9b5ce9d9f2113`에 연결했다.
- 최신 이력에 40개 문제, 답안·힌트·정답 비교, 저장 경계와 데스크톱·모바일 전수 검증 결과를 기록했다.
- 390×844 모바일 이력 화면에서 대표 이력 18개, 최신 커밋 `04c462b`, 갱신일 2026년 7월 26일을 확인했고 화면 밖 카드와 브라우저 오류는 0개였다.

### 임시 경로 정리와 원격 반영

- `/tmp`, `/private/tmp`, 현재 `$TMPDIR`, `/Users/seungik.yang/.codex/worktrees`에서 저장소 이름, `.git/config`, `.git` 연결 파일과 원격 URL을 다시 검색했다.
- 등록 worktree는 실제 저장소 루트 하나뿐이고 관련 임시 clone·worktree·일반 파일은 0개였다.
- 루트로 회수할 커밋·미커밋·미추적 작업과 삭제할 관련 임시 경로가 없었으므로 무관한 임시 파일은 삭제하지 않았다.
- `git fetch origin main` 뒤 로컬이 2커밋 앞서고 뒤처짐 0개임을 확인했다.
- 루트에서 강제 옵션 없이 `git push origin main`을 실행해 `a0d219a..b79445e` 범위를 원격에 반영했다.
- 다시 원격을 가져와 로컬 `HEAD`와 `origin/main`이 모두 `b79445efea04c417086596f754d134762f7109bf`로 일치하고 작업 트리가 깨끗함을 확인했다.

## 2026-07-26 중복 JavaScript 정리와 이력 갱신

### 시작 상태와 중복 감사

- 실제 저장소 루트와 물리 `pwd`는 `/Users/seungik.yang/Documents/company-work-system`으로 일치하고 루트 가드가 통과했다.
- 작업 시작 시 로컬 `main`, `origin/main`, `HEAD`는 `3f57506`으로 일치하고 작업 트리는 깨끗했다.
- 등록 worktree는 실제 저장소 루트 하나뿐이다.
- `python3 scripts/verify_workbook.py`는 HTML 링크 126개, Markdown 링크 41개, 프로그램 폴더 73개·학습 파일 77개와 대표 이력 18개 계약을 통과했다.
- `starter/`와 `hard/`의 대응 파일은 같은 개념을 난이도별로 직접 연습하는 의도된 중복이므로 유지한다.
- 실제 제거 대상은 `index.html`의 반복 DOM 조회, 프로필·오늘 학습 필드의 값 반영과 이벤트 연결, 다음 행동 카드 분기별 대입, 전체 코스 펼치기·접기와 배열 체크의 반복 로직이다.

### 설계 결정

- 프레임워크나 새 파일을 도입하지 않고 기존 인라인 JavaScript 안에서만 정리한다.
- DOM 요소는 시작 시 한 번 바인딩하고 기존 함수에서는 참조를 재사용한다.
- 상태 필드 공통 함수는 프로필과 오늘 학습 두 그룹만 처리하며 새로운 범용 상태 관리 계층은 만들지 않는다.
- 학습 콘텐츠, HTML 구조, 저장 키와 저장 데이터 형식은 변경하지 않는다.
- 검증 스크립트가 `getElementById` 연결과 중복 제거 함수의 존재를 검사하게 해 리팩터링이 되돌아가지 않도록 한다.

### 구현 결과

- `byId`와 프로필·오늘 학습 필드 맵을 추가해 알려진 ID 요소를 시작 시 한 번만 바인딩한다.
- 단계 체크, 세션 체크와 전체 코스 목록을 한 번 조회한 뒤 렌더링과 이벤트 처리에서 재사용한다.
- `syncFieldValues`와 `bindStateFields`로 프로필·오늘 학습의 값 복원과 입력 저장 반복 코드를 합쳤다.
- `setNextAction`으로 다음 행동 카드의 제목·설명·링크·버튼 문구 네 가지 반복 대입을 통합했다.
- `setCoursesOpen`으로 전체 펼치기와 접기의 코스 순회 로직을 하나로 합쳤다.
- `updateListValue`를 일반 진행 체크와 문제집 비교 체크가 함께 사용해 중복 `Set` 추가·삭제 처리를 제거했다.
- 인라인 JavaScript의 `document.querySelector`·`querySelectorAll` 호출은 65회에서 6회로 줄었다.
- 검증 스크립트는 `byId` 대상의 실제 DOM ID, 중복 제거 함수와 단계·세션·코스 목록의 단일 조회를 검사한다.

### 검증 결과

- `python3 scripts/verify_workbook.py`가 HTML 링크 126개, Markdown 링크 41개, 40개 챕터, 프로그램 폴더 73개·학습 파일 77개와 대표 이력 18개 계약을 통과했다.
- `git diff --check`가 통과했다.
- 실제 브라우저에서 프로필 입력, 오늘 FR ID·챕터·검증 명령·통과 결과가 각각 상태와 화면에 반영됨을 확인했다.
- 준비 단계와 세션 체크가 저장되고 단계 진행 수가 `1 / 5`로 갱신됨을 확인했다.
- 전체 펼치기는 5개 코스를 열고 전체 접기는 0개로 닫았다.
- 문제집 비교 체크의 추가·삭제·재추가가 `1 / 40 → 0 / 40 → 1 / 40`으로 정확히 반영됐다.
- 오늘 기록 초기화 뒤 오늘 필드와 세션 체크는 비워지고 프로필·단계·문제집 기록은 유지됐다.
- 브라우저 검증 데이터를 모두 지운 뒤 새로고침해 목표·단계·오늘 챕터·문제집 진행 수가 초기 상태임을 확인했다.
- 브라우저 오류 로그는 0개였다.

### 기능 커밋과 이력 연결

- 루트에서 `26f6bed refactor: 워크북 DOM 중복 조회 정리` 커밋을 생성했다.
- `history.html`의 대표 이력을 19개로 늘리고 최신 항목을 실제 기능 커밋 `26f6bed6997a6a27e04f5eee2bd4563d224c3c73`에 연결했다.
- 최신 이력에 DOM 조회 65회에서 6회로 감소한 결과와 상태 연결 공통화, 정적·브라우저 회귀 검사 통과를 기록했다.

### 원격 반영 결과

- 루트에서 `c3d9eca docs: 중복 코드 정리 이력 동기화` 커밋을 생성했다.
- `git fetch origin main` 뒤 로컬이 2커밋 앞서고 뒤처짐 0개임을 확인했다.
- 강제 옵션 없이 `git push origin main`을 실행해 `3f57506..c3d9eca` 범위를 원격에 반영했다.
- 다시 원격을 가져와 로컬 `HEAD`와 `origin/main`이 모두 `c3d9ecac71bf1294b9a28a14aee33b3692809057`로 일치함을 확인했다.

## 2026-07-27 Orca 고정 URI 실행

### 시작 상태와 요구 해석

- 실제 저장소 루트와 물리 `pwd`는 `/Users/seungik.yang/Documents/company-work-system`으로 일치하고 루트 가드가 통과했다.
- 작업 시작 시 로컬 `main`, `origin/main`, `HEAD`는 `9e51eb9`로 일치하고 작업 트리는 깨끗했다.
- 등록 worktree는 실제 저장소 루트 하나뿐이고 `4173` 포트에는 수신 중인 프로세스가 없었다.
- 사용자가 말한 폴더 이름 URI는 폴더명 경로와 고정 포트를 함께 유지하는 주소로 해석한다.
- 고정 URI는 포트와 경로가 매번 같다는 의미로 적용하며, 컴퓨터 재시작 뒤에는 저장소의 실행 명령을 다시 실행해 같은 주소를 복원한다.

### 설계 결정

- 상위 `Documents` 디렉터리를 그대로 정적 제공하면 다른 폴더가 노출되므로 사용하지 않는다.
- 저장소 루트만 파일 원본으로 삼고 `/company-work-system/` 접두사만 허용하는 Python 표준 라이브러리 서버를 둔다.
- 실행 스크립트는 고정 URI가 이미 정상 응답하면 서버를 재사용하고, 포트가 다른 프로세스에 점유됐으면 종료하지 않고 명확히 실패한다.
- Orca는 `/Applications/Orca.app/Contents/Resources/bin/orca`의 공식 `tab create --url` 명령을 사용한다.

### 포트 충돌과 최종 주소

- 최초 감사 시점에는 `4173` 수신 프로세스가 없었지만 서버 동적 검증 직전에 `Address already in use` 오류가 발생했다.
- 실제 `lsof`, `ps`와 작업 디렉터리를 확인해 PID `38440`이 다른 저장소 `/Users/seungik.yang/Documents/accountProgram`에서 `python3 -m http.server 4173`을 실행 중임을 확인했다.
- 범위 밖 프로세스는 중단하지 않고 사용 가능한 `4174`를 이 저장소의 고정 포트로 확정했다.
- 최종 고정 URI는 `http://127.0.0.1:4174/company-work-system/`이다.
- 실제 `index.html`의 제목은 `Company Work System 취업 워크북`이므로 서버 준비와 Orca 로드 검사는 이 제목을 기준으로 한다.

### 구현 결과

- `scripts/serve_workbook.py`에 `127.0.0.1:4174` 전용 서버를 추가하고 저장소 폴더 이름을 URI 접두사로 사용했다.
- `/company-work-system`은 trailing slash가 있는 고정 주소로 308 이동하고 `/company-work-system/`은 `index.html`을 반환한다.
- 공개 범위를 루트의 워크북 문서와 `practice/` 파일로 제한하고 `.git`, `scripts`, 다른 루트 파일과 디렉터리 목록은 404로 차단했다.
- `scripts/open_workbook_in_orca.sh`는 Orca 공식 CLI를 열고 고정 URI 응답을 확인한 뒤 서버가 없을 때만 Orca 관리 터미널에서 시작한다.
- 같은 스크립트를 다시 실행하면 기존 서버를 재사용하고 동일 URI의 새 Orca 탭을 연다.
- 검증 스크립트에 서버 Python 구문, 고정 호스트·포트·폴더 접두사·공개 범위와 Orca 실행 셸 구문·명령 계약을 추가했다.

### HTTP와 Orca 반복 검증

- 전용 서버에서 `/company-work-system/`은 200, trailing slash 없는 주소는 고정 URI로 308 이동했다.
- `/`, `/accountProgram/`, `.git/config`, `scripts/serve_workbook.py`, `practice/` 디렉터리 목록은 모두 404를 반환했다.
- 공개 학습 문서 `practice/answers.md`는 200을 반환했다.
- `./scripts/open_workbook_in_orca.sh`를 두 번 연속 실행해 두 실행 모두 `http://127.0.0.1:4174/company-work-system/`을 열고 `4174` 수신 프로세스는 하나만 유지됨을 확인했다.
- 최종 Orca 탭의 제목은 `Company Work System 취업 워크북`, `active`는 `true`, `loadError`와 `certificateFailure`는 `null`이었다.
- 반복 검증 중 만든 이전 브라우저 탭은 닫고 최종 고정 URI 탭 하나를 활성 상태로 남겼다.
- `sh -n scripts/open_workbook_in_orca.sh`, `python3 scripts/verify_workbook.py`와 `git diff --check`가 통과했다.

### 기능 커밋과 이력 연결

- 루트에서 `670c257 feat: Orca 고정 URI 워크북 실행 추가` 커밋을 생성했다.
- `history.html`의 대표 이력을 20개로 늘리고 최신 항목을 실제 기능 커밋 `670c2575e07decdfc97bb6607249c357631e62de`에 연결했다.
- 최신 이력에 고정 URI, 서버 재사용, 공개 범위 제한과 HTTP·Orca 실제 검증 결과를 기록했다.

### 원격 반영 결과

- 루트에서 `b11b4ef docs: Orca 고정 URI 이력 기록` 커밋을 생성했다.
- `git fetch origin main` 뒤 로컬이 2커밋 앞서고 뒤처짐 0개임을 확인했다.
- 강제 옵션 없이 `git push origin main`을 실행해 `9e51eb9..b11b4ef` 범위를 원격에 반영했다.
- 다시 원격을 가져와 로컬 `HEAD`와 `origin/main`이 모두 `b11b4ef2309b4bac6ba4602c0ac28f33356a6d61`로 일치함을 확인했다.

## 2026-07-28 워크북 UI·화면 전환·UX 폐쇄 루프

### 시작 상태와 기준선

- 실제 저장소 루트와 물리 `pwd`는 `/Users/seungik.yang/Documents/company-work-system`으로 일치하고 루트 가드가 통과했다.
- 작업 시작 시 로컬 `main`, `origin/main`, `HEAD`는 `661ad5c`로 일치하고 작업 트리는 깨끗했다.
- 등록 worktree는 실제 저장소 루트 하나뿐이고 `.openai/hosting.json`은 없어 기존 정적 HTML과 로컬 고정 URI 실행 방식을 유지한다.
- `python3 scripts/verify_workbook.py`는 HTML 링크 126개, Markdown 링크 41개, 40개 챕터, 프로그램 폴더 73개·학습 파일 77개와 대표 이력 20개 계약을 통과했다.
- 데스크톱 1967×1179의 문서 너비와 스크롤 너비는 1952px로 같고 화면 밖 컨트롤은 없었다.
- 모바일 390×844의 문서 너비와 스크롤 너비는 375px로 같고 화면 밖 컨트롤은 없었다.

### 재현한 UI·화면 전환 문제

- 모바일 상단 메뉴가 3줄로 줄바꿈되어 고정 헤더 높이가 116.5px까지 늘어난다.
- 모바일에서 `내 목표` 메뉴를 누르면 `#setup`과 제목 상단이 0px에 놓여 고정 헤더 아래에서 제목 전체가 가려진다.
- `오늘 학습` 메뉴에서도 제목 상단 70.4px가 116.5px 헤더와 겹쳐 일부가 가려진다.
- 10장을 선택하면 제목·문제·연속 탐색 데이터는 정확히 갱신되지만 선택 챕터 패널 상단이 화면보다 약 1,161px 아래에 남아 전환 결과를 바로 찾기 어렵다.
- 사이트 공통 포커스 스타일은 링크와 `summary`에만 있고 일반 버튼은 구성 요소별 스타일에 의존한다.
- `html`의 부드러운 스크롤과 여러 전환 효과에 `prefers-reduced-motion` 예외가 없다.

### 설계 결정

- 학습 콘텐츠, 저장 키와 데이터 형식, 5단계·40개 챕터 구조는 바꾸지 않는다.
- 모바일 메뉴는 항목을 숨기는 햄버거 대신 한 줄 가로 스크롤로 유지해 모든 섹션을 한 번에 탐색 가능하게 한다.
- 챕터 선택 즉시 강제 스크롤하지 않고 선택 필드 아래의 명시적 이동 버튼으로 사용자가 전환 시점을 결정하게 한다.
- 이전·다음 버튼은 이미 선택 패널 안에서 사용하는 탐색이므로 전환 뒤 제목에 포커스를 이동한다.
- 현재 메뉴 표시는 내부 앵커 이동과 주소 해시를 기준으로 갱신해 복잡한 스크롤 감지 계층을 추가하지 않는다.

### 구현과 수정 루프

- 섹션과 선택 챕터 패널에 고정 헤더를 고려한 `scroll-margin-top`을 적용했다.
- 모바일 메뉴를 줄바꿈 없는 가로 스크롤 칩으로 바꾸고 시각적 스크롤바는 숨기되 터치·휠 탐색은 유지했다.
- 현재 주소 해시와 일치하는 내부 메뉴에 `aria-current="location"`을 적용하고 프로그램 전환도 해시와 메뉴를 함께 갱신한다.
- 오늘 챕터 선택 필드 아래에 선택 전에는 비활성화되고 선택 뒤 챕터 번호·제목을 표시하는 문제 이동 버튼을 추가했다.
- 문제 이동과 이전·다음 전환은 선택 챕터 패널을 헤더 아래로 이동한 뒤 제목에 포커스를 전달한다.
- 일반 버튼의 공통 `:focus-visible` 스타일과 `prefers-reduced-motion` CSS·JavaScript 분기를 추가했다.
- 1차 재검증에서 가로 메뉴 스크롤바 때문에 모바일 헤더가 99.3px로 남고 제목이 7px 가려지는 문제를 발견했다.
- 2차 수정에서 스크롤 기능은 유지한 채 막대를 숨기고 모바일 앵커 여백을 100px로 늘려 헤더를 88.3px로 줄였다.
- 2차 재검증에서 문제 이동 뒤 현재 메뉴가 이전 해시에 남는 문제를 발견해 프로그램 이동 시 `#session`·`#curriculum`과 메뉴를 함께 동기화했다.

### 브라우저 반복 검증 결과

- 모바일 390×844에서 문서 너비와 스크롤 너비는 375px로 같고 고정 헤더는 88.3px, 메뉴는 `nowrap`·가로 스크롤 상태였다.
- `내 목표` 이동 뒤 제목 상단은 100.2px, `오늘 학습` 제목 상단은 170.6px로 88.3px 헤더 아래에 완전히 표시됐다.
- 10장 문제 이동 뒤 패널 상단은 112.9px, 활성 요소는 `selected-chapter-title`, 현재 메뉴와 해시는 `오늘 학습`·`#session`이었다.
- 다음 챕터를 누르면 선택 값 `11`, 제목·문제 11장, 위치 `12 / 40`, 제목 포커스와 `오늘 학습` 메뉴가 함께 갱신됐다.
- `전체 목차에서 이 챕터 보기`는 해당 코스를 열고 11장 행을 화면의 329.6~514.9px에 표시했으며 현재 메뉴와 해시는 `전체 교재`·`#curriculum`이었다.
- 데스크톱 1967×1179에서 문서 너비와 스크롤 너비는 1952px로 같고 헤더는 65px, `내 목표` 제목 상단은 87.8px이었다.
- 키보드 Enter로 전체 코스를 5개 펼치고 0개로 접었으며 활성 버튼의 `:focus-visible`은 참, 외곽선은 3px 실선이었다.
- 프로필·오늘 학습·단계·세션·문제 답안·비교 완료를 작성하고 새로고침해 모든 값과 `1 / 40` 진행 수가 복원됨을 확인했다.
- 힌트·정답 기준을 각각 열고 `aria-expanded=true`, 패널 표시와 `정답 비교 완료` 상태를 확인했다.
- 오늘 기록 초기화 뒤 오늘 필드·세션은 비워지고 프로필·단계는 유지되는 경계를 확인한 뒤 모든 검증 데이터를 UI로 원상 복구했다.
- 원상 복구 후 목표·오늘 챕터는 빈 값, 단계 `0 / 5`, 챕터 `0 / 40`, 문제집 `0 / 40`이며 다음 행동은 `먼저 목표 직무를 적으세요.`였다.
- 데스크톱과 모바일 브라우저 경고·오류 로그는 모두 0개였다.
- `python3 scripts/verify_workbook.py`와 `git diff --check`가 통과했다.

### 기능 커밋과 이력 연결

- 루트에서 `0491df4 feat: 워크북 화면 전환 UX 개선` 커밋을 생성했다.
- `history.html`의 대표 이력을 21개로 늘리고 최신 항목을 실제 기능 커밋 `0491df45175349c4a4293966e3b294692e234e56`에 연결했다.
- 최신 이력에 모바일 헤더 축소, 앵커·챕터 포커스, 키보드·모션 접근성과 저장·초기화 회귀 결과를 기록했다.
- 데스크톱 1967×1179과 모바일 390×844 이력 화면에서 대표 이력 21개, 최신 커밋 `0491df4`, 갱신일 2026년 7월 28일과 가로 넘침 0을 확인했다.
- 이력 화면의 데스크톱·모바일 브라우저 경고·오류 로그는 모두 0개였다.

### 원격 반영 결과

- 루트에서 `c794e53 docs: 워크북 UX 개선 이력 기록` 커밋을 생성했다.
- `git fetch origin main` 뒤 로컬이 2커밋 앞서고 뒤처짐 0개임을 확인했다.
- 강제 옵션 없이 `git push origin main`을 실행해 `661ad5c..c794e53` 범위를 원격에 반영했다.
- 다시 원격을 가져와 로컬 `HEAD`와 `origin/main`이 모두 `c794e53e2aec98ccc9abcc17b56b2d6dedd64ea4`로 일치함을 확인했다.
