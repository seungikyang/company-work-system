# 저장소 루트 작업 규칙

## 필수 작업 위치

- 이 저장소의 유일한 작업 위치는 `git rev-parse --show-toplevel`이 반환하는 실제 저장소 루트다.
- 파일 수정, 테스트, 빌드, 커밋, 푸시처럼 상태에 영향을 주는 작업 전에는 `pwd -P`가 저장소 루트의 물리 경로와 같은지 확인한다.
- 현재 위치가 루트가 아니면 작업을 시작하지 말고 저장소 루트로 이동한다.
- Git 명령은 저장소 루트에서 실행하며, 다른 디렉터리의 Git 메타데이터나 작업 트리를 대신 사용하지 않는다.

## 임시 작업 금지

- `/tmp`, `/private/tmp`, `$TMPDIR`, `~/.codex/worktrees` 아래에 이 저장소의 clone이나 worktree를 만들거나 사용하지 않는다.
- 임시 clone, 임시 worktree, 복사본에서는 파일 수정, 테스트, 빌드, 커밋, 푸시를 하지 않는다.
- 기존 임시 작업을 발견하면 읽기 전용으로 비교하고, 필요한 내용은 저장소 루트에 명시적으로 반영한 뒤 루트에서 검증한다.
- 루트 `.git` 쓰기나 네트워크가 제한되더라도 임시 저장소로 우회하지 않는다. 루트의 변경을 보존하고 실제 오류와 필요한 사용자 조치를 보고한다.

## 시작 전 확인 명령

```sh
repo_root=$(git rev-parse --show-toplevel)
test "$(pwd -P)" = "$(cd "$repo_root" && pwd -P)"
```

두 번째 명령이 실패하면 이후 작업을 중단하고 저장소 루트로 이동한다.
