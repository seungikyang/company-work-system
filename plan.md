# 취업 워크북 프로그램 보완 계획

## 목적

현재 저장소의 기술 실습 자산은 유지하면서, 채용 공고 분석부터 구현·검증·포트폴리오·면접·지원까지 하나의 워크북 흐름으로 추적되도록 문서를 보완한다.

## 작업 범위

1. 루트 안내와 통합 워크북에 취업 준비 단계, 단계별 통과 기준, 증거 ID 규칙을 추가한다.
2. PRD/TRD와 기능·문제·해설·난이도별 안내 문서가 같은 증거 체계를 사용하도록 맞춘다.
3. 보안·문서화·화면·구조·규칙·트러블슈팅·면접 워크시트에 산출물 기록란을 보완한다.
4. `history.html`을 실제 Git 커밋과 이번 변경 내용에 맞게 갱신한다.
5. 문서 링크, 표 구조, HTML 구조, Git diff를 검증한 뒤 의미 단위로 커밋하고 `origin/main`에 푸시한다.

## 제외 범위

- `starter/`와 `hard/`의 Java 코드 조각을 실행 가능한 Spring Boot 프로젝트로 변환하지 않는다.
- PRD에 없는 신규 업무 기능을 추가하지 않는다.
- 학습자가 직접 작성해야 하는 빈칸에 모범 답안을 미리 채우지 않는다.

## 성공 기준

- 처음 방문한 사용자가 README에서 현재 단계와 다음 행동을 결정할 수 있다.
- 채용 공고 요구사항, FR ID, 코드, 테스트, 문서, 면접 답변을 하나의 증거 ID로 추적할 수 있다.
- 모든 Markdown 문서가 취업 워크북 안에서 자신의 역할과 완료 산출물을 설명한다.
- `history.html`의 커밋 수, 해시, 설명이 실제 Git 이력과 일치한다.
- 문서와 HTML 검증 명령이 통과하고 변경사항이 원격 `main`에 반영된다.

## 2026-07-16 코드와 문서 전수 감사 계획

### 목적

문서 중심으로 정리된 취업 워크북이 실제 코드·설정 조각과도 일치하는지 전수 확인하고, 학습자가 세션 인증·보안 의존성·통합 테스트를 서로 충돌 없이 구현 프로젝트로 옮길 수 있게 한다.

### 작업 범위

1. Git 추적 파일 90개를 문서, HTML, Java 조각, 설정 조각으로 나눠 확인한다.
2. Starter 35개와 Hard 35개의 경로 대응, 학습 목표, TODO·빈칸 구조를 검사한다.
3. 코드 주석의 존재하지 않는 참조와 PRD/TRD의 enum·API·ErrorCode·계층 책임 충돌을 수정한다.
4. 사용자 문서 15개와 내부 기록 문서 3개의 링크, 표, 취업 목적, 완료 증거 안내를 재검증한다.
5. 감사 결과를 실제 Git 커밋 기반 `history.html`에 추가하고 원격 `main`에 배포한다.

### 성공 기준

- 70개 코드·설정 조각이 Starter·Hard 범위와 학습 구조 검사를 통과한다.
- 40개 챕터와 사용자 문서 15개가 취업 워크북 목적 및 실행 증거 흐름을 유지한다.
- 1차 세션 인증의 Controller·Service 책임, MockMvc 세션 재사용, 보안 의존성이 문서와 코드에서 일치한다.
- Markdown 링크·표, Java 조각 구조, HTML 구조, Git diff 검사가 통과한다.
- 의미 단위 커밋과 `history.html` 업데이트가 `origin/main`에 반영된다.

## 2026-07-17 HTML 학습 목차 구축 계획

### 목적

처음 방문한 취업 준비생이 Markdown 파일 경로를 스스로 탐색하지 않아도 브라우저에서 현재 단계와 오늘 할 일을 선택하고, 40개 기술 챕터와 구현·검증·취업 증거 기록 문서로 바로 이동하게 한다.

### 작업 범위

1. 루트에 정적 `index.html` 학습 목차를 추가한다.
2. 준비·설계·구현·검증·지원의 5단계와 한 번의 학습 세션 순서를 시각적으로 연결한다.
3. 00~39 챕터를 주제별로 묶고 문제·Starter·Hard·해설 진입점을 제공한다.
4. 루트 README, 학습 워크북 안내, 변경 히스토리에서 HTML 목차로 이동하게 한다.
5. 링크·HTML 구조·반응형 화면을 검증하고 실제 구현 커밋을 `history.html`에 기록한다.

### 제외 범위

- Markdown 워크북과 학습용 Java 조각을 실행 애플리케이션으로 변환하지 않는다.
- 기존 40개 챕터의 기술 내용과 정답은 변경하지 않는다.
- 별도 서버, 프레임워크, 빌드 도구를 도입하지 않는다.

### 성공 기준

- `index.html`만 열어도 프로그램 목적, 시작 순서, 5단계 진행 기준을 이해할 수 있다.
- 40개 챕터가 빠짐없이 표시되고 각 챕터의 실제 학습 파일로 이동할 수 있다.
- 로컬 링크, HTML 태그·ID·CSS 구조와 모바일·데스크톱 렌더링이 정상이다.
- 변경사항이 의미 단위 커밋으로 기록되고 `origin/main`에 반영된다.

## 2026-07-17 따라 쓰는 취업 워크북 대시보드 개편 계획

### 목적

처음 방문한 취업 준비생이 자료 목록에서 길을 찾는 대신 목표를 적고, 오늘 할 일을 고르고, 완료한 증거를 체크하면서 준비·설계·구현·검증·지원 순서를 그대로 따라가게 한다.

### 작업 범위

1. `index.html` 상단을 목표 설정과 현재 진행 상황을 보여주는 워크북 대시보드로 재구성한다.
2. 목표 직무·마감일·하루 학습 시간과 오늘의 FR ID·챕터를 브라우저에 저장하고 새로고침 뒤 복원한다.
3. 5단계 로드맵과 00~39 챕터에 완료 체크를 연결하고 전체 진척도를 표시한다.
4. 기존 Markdown과 Starter·Hard 링크는 유지하면서 처음 시작하는 행동을 더 짧고 분명하게 안내한다.
5. README와 `history.html`을 실제 변경 내용과 Git 이력에 맞춘 뒤 검증·커밋·푸시한다.

### 제외 범위

- 학습용 Java·설정 조각의 기술 내용과 정답은 변경하지 않는다.
- 로그인, 서버, 데이터베이스, 외부 분석 도구를 추가하지 않는다.
- 브라우저 밖의 여러 기기 간 진행 상태 동기화는 제공하지 않는다.

### 성공 기준

- 첫 화면에서 목표 설정, 다음 행동, 전체 진척도를 바로 확인할 수 있다.
- 입력과 체크 상태가 현재 브라우저에 저장되고 새로고침 후 복원된다.
- 준비·설계·구현·검증·지원 단계마다 행동, 산출물, 통과 기준이 명확하다.
- 00~39 챕터와 기존 로컬 링크가 빠짐없이 유지된다.
- 정적 검사와 데스크톱·모바일 브라우저 검증이 통과한다.
- 의미 단위 커밋과 `history.html` 갱신이 `origin/main`에 반영된다.

## 2026-07-17 임시 작업 회수와 루트 작업 고정 계획

### 목적

임시 Git 저장소에 남은 대시보드 작업을 프로젝트 루트의 파일과 대조해 누락 없이 배포하고, 이후 Codex 작업이 임시 clone이나 worktree가 아닌 저장소 루트에서만 이루어지게 한다.

### 작업 범위

1. 등록된 worktree와 임시 디렉터리의 Git 저장소·커밋·변경 파일을 확인한다.
2. 임시 커밋과 루트 작업 파일을 blob 단위로 비교해 동일성을 검증한다.
3. 루트 `AGENTS.md`에 루트 경로 확인과 임시 clone·worktree 사용 금지 규칙을 추가한다.
4. 정적 검사와 Git diff 검사를 다시 실행한다.
5. 대시보드 변경과 루트 작업 고정 규칙을 의미 단위로 원격 `main`에 반영한다.

### 성공 기준

- 임시 커밋과 루트 작업본의 추적 파일 91개가 일치한다.
- 이후 에이전트가 파일 수정·테스트·커밋·푸시 전에 실제 저장소 루트인지 확인한다.
- 임시 경로의 clone이나 worktree를 작업 우회 수단으로 사용하지 않는다.
- 검증한 변경만 원격 `main`에 반영되고 남은 제약이 명확히 기록된다.

## 2026-07-19 따라가는 학습·구현 루프 고도화 계획

### 목적

처음 방문한 취업 준비생이 문서 위치를 추측하지 않고 HTML 목차에서 목표 설정, 챕터 선택, 직접 풀이, 실행 프로젝트 구현, 테스트와 취업 증거 기록까지 한 방향으로 따라가게 한다.

### 작업 범위

1. `index.html`에 전체 워크북 순서를 한눈에 보여주는 번호형 학습 목차를 추가한다.
2. 오늘 선택한 챕터의 문제, Hard, Starter와 증거 기록 문서를 바로 여는 학습 실행 패널을 연결한다.
3. 기존 5단계와 40개 챕터, 브라우저 저장 상태와 모든 학습 파일 링크를 그대로 유지한다.
4. HTML 구조, 링크와 fragment, JavaScript, 진행 상태 계약을 한 명령으로 반복 검증하는 스크립트를 추가한다.
5. 데스크톱과 모바일 브라우저에서 입력, 선택, 완료, 새로고침 복원을 검증하고 실패하면 수정 후 재검증한다.
6. README와 `history.html`을 실제 구현·검증 결과에 맞춘 뒤 의미 단위로 커밋하고 `origin/main`에 푸시한다.

### 제외 범위

- 학습용 Java 조각의 정답과 업무 규칙은 변경하지 않는다.
- 서버, 로그인, 데이터베이스나 외부 JavaScript 라이브러리를 추가하지 않는다.
- 사용자의 실제 Spring Boot 프로젝트를 이 정적 워크북 안에서 자동 생성하지 않는다.

### 성공 기준

- 첫 화면에서 전체 학습 순서와 각 단계의 산출물을 이해하고 해당 위치로 바로 이동할 수 있다.
- 챕터를 고르면 문제, 직접 풀이, 힌트 확인, 본인 프로젝트 구현과 증거 기록 순서가 표시된다.
- 선택 챕터의 실제 Starter·Hard·워크시트 링크가 화면에 자동 연결된다.
- 목표·오늘 학습·단계·챕터·세션 상태가 저장되고 새로고침 후 복원된다.
- 반복 가능한 검증 명령과 실제 데스크톱·모바일 브라우저 검증이 모두 통과한다.
- `history.html`과 Git 이력이 실제 변경을 반영하고 로컬 `HEAD`와 `origin/main`이 일치한다.

## 2026-07-19 반복 가능한 검증 폐쇄 루프 계획

### 목적

오늘 학습에서 실행한 테스트 명령과 실패·통과 결과를 기록하고, 실패하면 같은 명령으로 다시 검증하며, 다음 학습에서는 장기 진척도를 보존한 채 오늘 기록만 새로 시작할 수 있게 한다.

### 작업 범위

1. 오늘 학습 상태에 실제 검증 명령과 검증 결과를 추가하고 기존 브라우저 저장 상태와 호환한다.
2. 미실행·실패·통과 상태마다 다음 행동을 한 문장으로 안내한다.
3. 목표·단계·완료 챕터는 유지하면서 오늘의 FR ID·챕터·완료 조건·검증과 세션 체크만 초기화하는 버튼을 추가한다.
4. 정적 검증 계약에 새 입력·상태·초기화 요소를 포함한다.
5. 데스크톱과 모바일에서 실패→통과→새로고침 복원→새 학습 시작을 실제로 검증한다.
6. README와 `history.html`을 실제 결과에 맞추고 커밋·푸시한다.

### 성공 기준

- 사용자가 실행할 테스트 명령과 미실행·실패·통과 결과를 오늘 학습에 기록할 수 있다.
- 실패 상태에서는 오류 기록과 최소 수정 후 같은 명령 재실행을 안내한다.
- 통과 상태에서는 회귀 테스트와 증거 기록을 안내한다.
- 새 학습 시작 후 오늘 기록과 세션 체크만 비워지고 목표·단계·완료 챕터는 유지된다.
- 기존 저장 데이터가 오류 없이 로드되고 새 상태가 새로고침 뒤 복원된다.
- 정적·브라우저 검증과 원격 배포가 모두 통과한다.

## 2026-07-24 폴더별 취업 결과물 계약과 전수 검증 계획

### 목적

Starter 40개와 Hard 33개 프로그램 폴더를 HTML 목차에서 빠짐없이 따라가고, 각 챕터를 읽기 완료가 아니라 채용 담당자에게 제시할 결과물과 실제 검증으로 끝내게 한다.

### 작업 범위

1. 모든 챕터에 취업 결과물과 완료 검증 기준을 연결하고 선택 챕터 패널에서 바로 확인하게 한다.
2. 기존 문제 → Hard → Starter → 실행 프로젝트 구현 → 증거 기록 순서를 유지한다.
3. 정적 검증기를 73개 프로그램 폴더와 그 안의 77개 학습 파일까지 확장한다.
4. 폴더 번호·Starter/Hard 대응·워크북 표식·HTML 전수 링크를 반복 검사한다.
5. 데스크톱과 모바일 브라우저에서 챕터 선택, 결과물 계약, 링크와 반응형 화면을 검증한다.
6. README와 `history.html`을 실제 변경·검증 결과에 맞춰 갱신하고 의미 단위로 커밋한다.

### 제외 범위

- 학습용 조각을 실행 가능한 단일 Spring Boot 애플리케이션으로 합치지 않는다.
- 학습자가 직접 풀어야 하는 TODO와 빈칸에 정답을 채우지 않는다.
- PRD/TRD에 없는 업무 기능이나 외부 라이브러리를 추가하지 않는다.

### 성공 기준

- 00~39 모든 챕터에서 취업 결과물과 완료 검증을 선택 패널로 확인할 수 있다.
- Starter 40개, Hard 33개 폴더의 학습 파일 77개가 비어 있지 않고 HTML에서 모두 연결된다.
- Hard 코드 챕터가 같은 번호의 Starter 파일과 정확히 대응한다.
- 정적 검사와 데스크톱·모바일 브라우저 검증이 모두 통과한다.
- `history.html`과 작업 기록이 실제 구현과 검증 결과를 반영한다.

## 2026-07-24 연속 학습 탐색과 파일 내부 계약 계획

### 목적

각 프로그램 폴더를 HTML 목차에서 순서대로 따라가고, 파일을 직접 열어도 학습 목표·취업 결과물·완료 검증을 놓치지 않게 한다.

### 재감사 결과

- 코드·설정 조각 70개는 모두 파일 상단에 `목표`가 있다.
- 서술형 워크시트 7개는 목적과 완료 증거를 본문으로 설명하지만 공통 형식의 챕터 계약이 없다.
- HTML 선택 패널은 40개 챕터를 연결하지만 이전·다음 챕터로 연속 이동하는 조작이 없다.

### 작업 범위

1. 서술형 워크시트 7개에 학습 목표·취업 결과물·완료 검증 챕터 계약을 추가한다.
2. HTML 선택 패널에 현재 위치와 이전·다음 챕터 탐색을 추가한다.
3. 00과 39의 경계, 오늘 학습 초기화와 기존 저장 상태 호환성을 유지한다.
4. 검증기에 코드·설정 목표 헤더, Markdown 챕터 계약, 챕터 번호별 폴더 링크 대응을 추가한다.
5. 데스크톱·모바일에서 00→39 순차 이동과 경계 상태를 실제로 검증한다.
6. README와 `history.html`을 실제 결과에 맞춰 갱신하고 의미 단위로 커밋한다.

### 성공 기준

- 프로그램 파일 77개가 내부 목표 또는 챕터 계약을 갖는다.
- 선택 챕터에서 현재 위치와 이전·다음 행동을 바로 알 수 있다.
- 00에서는 이전, 39에서는 다음 이동이 비활성화되고 중간 챕터는 양방향 이동한다.
- 정적 검사와 데스크톱·모바일 브라우저 검증이 모두 통과한다.
- 문서와 변경 이력이 실제 구현·검증 결과와 일치한다.

## 2026-07-24 임시 작업 감사·루트 가드·원격 반영 계획

### 목적

임시 경로에 남은 이 저장소 작업을 확인·회수·정리하고, 이후 Codex의 상태 변경 작업이 실제 저장소 루트에서만 시작되도록 실행 가능한 가드를 둔 뒤 루트 `main`을 원격에 반영한다.

### 확인 범위

1. 실제 루트, 브랜치, 원격, 등록 worktree와 로컬·원격 차이를 확인한다.
2. `/tmp`, `/private/tmp`, `$TMPDIR`, `~/.codex/worktrees`의 관련 이름·Git 메타데이터·원격 문자열을 읽기 전용으로 검색한다.
3. 관련 임시 작업이 있으면 커밋·미커밋·미추적 파일을 루트와 대조한 뒤에만 삭제한다.
4. 루트와 금지 경로를 검사하는 실행 가드를 추가하고 `AGENTS.md` 시작 게이트에 연결한다.
5. 루트에서 전체 검증·커밋·푸시 후 `HEAD`와 `origin/main`을 확인한다.

### 성공 기준

- 등록 worktree가 실제 루트 하나뿐이다.
- 임시 경로에 루트로 회수되지 않은 변경이 없다.
- 관련 임시 clone·worktree·파일이 0개다.
- 루트 가드와 워크북 검증이 통과한다.
- 강제 옵션 없이 `origin/main`에 반영되고 로컬·원격 커밋이 일치한다.

## 2026-07-26 챕터별 문제집 상호작용과 학습 검증 계획

### 목적

00~39 프로그램 폴더를 파일 링크 모음이 아니라, 선택한 챕터에서 직접 답을 쓰고 힌트·정답 기준을 단계적으로 확인하며 취업용 설명까지 남기는 문제집으로 완성한다.

### 재감사 결과

- 루트 HTML은 6단계 목차, 5단계 로드맵, 40개 챕터, 학습 파일과 검증 결과 기록을 이미 제공한다.
- `practice/problems.md`와 `practice/answers.md`에는 챕터별 문제와 해설이 있지만 HTML 안에서 답을 작성하거나 힌트·정답 기준을 개별 확인할 수 없다.
- 현재 브라우저 저장 상태에는 목표·오늘 학습·완료 챕터가 있지만 챕터별 답안과 자가 점검 기록은 없다.
- 기존 검증은 구조·링크·JavaScript 구문을 확인하지만 문제집 상호작용 계약은 검사하지 않는다.

### 작업 범위

1. 선택 챕터 패널에 챕터별 취업형 회상 문제와 답안 입력란을 추가한다.
2. 취업 결과물에 기반한 힌트와 완료 검증에 기반한 정답 기준을 별도 버튼으로 확인하게 한다.
3. 챕터별 답안과 비교 완료 기록을 기존 브라우저 저장 상태와 호환되게 보존한다.
4. 문제집 진행 수를 대시보드에 표시하고 새 학습 초기화와 장기 학습 기록의 경계를 유지한다.
5. 검증 스크립트에 문제집 DOM·저장·토글·40개 챕터 연결 계약을 추가한다.
6. 데스크톱·모바일 브라우저에서 작성·힌트·정답·저장 복원·챕터 이동·초기화를 반복 검증한다.
7. README, 학습 안내, 체크리스트, 컨텍스트 노트와 `history.html`을 실제 결과에 맞춰 갱신한다.

### 성공 기준

- 00~39 어느 챕터를 선택해도 해당 제목·설명·취업 결과물·검증 기준으로 구성된 문제가 표시된다.
- 답안을 작성한 뒤 힌트와 정답 기준을 독립적으로 열고 닫을 수 있다.
- 챕터를 이동하거나 새로고침해도 각 답안과 비교 완료 기록이 복원된다.
- 오늘 기록만 비워도 장기 문제집 기록은 유지된다.
- 정적 검증, JavaScript 구문 검사, 데스크톱·모바일 상호작용 검증과 Git diff 검사가 모두 통과한다.
- 관련 임시 작업을 루트와 대조한 뒤 안전하게 정리하고 루트 `main`을 강제 옵션 없이 푸시한다.

## 2026-07-26 중복 JavaScript 정리와 이력 갱신 계획

### 목적

워크북 동작과 학습 콘텐츠는 유지하면서 `index.html`에서 같은 DOM 요소 조회와 상태 반영을 반복하는 코드를 한 곳으로 모으고, 실제 리팩터링 결과를 `history.html`과 Git 이력에 연결한다.

### 감사 결과

- `starter/`와 `hard/`의 유사 코드는 난이도별 직접 풀이를 위한 의도된 학습 자산이므로 중복 제거 대상이 아니다.
- 프로필·오늘 학습 필드는 초기 렌더링과 이벤트 연결에서 같은 선택자로 반복 조회된다.
- 단계·세션 체크와 전체 코스도 렌더링·이벤트·펼치기 동작에서 각각 다시 조회된다.
- 다음 행동 카드의 제목·설명·링크·버튼 문구가 네 분기에서 반복 대입된다.
- 기존 `python3 scripts/verify_workbook.py`는 전체 계약을 통과하고 작업 트리와 원격 `main`은 일치한다.

### 작업 범위

1. 자주 쓰는 ID 요소와 요소 목록을 최초 한 번만 조회해 재사용한다.
2. 프로필·오늘 학습 필드의 값 동기화와 입력 이벤트 연결을 공통 함수로 합친다.
3. 다음 행동 카드 갱신과 전체 코스 펼치기·접기 반복 로직을 공통 함수로 합친다.
4. 배열 체크 상태 갱신의 중복 `Set` 처리를 하나의 함수로 통합한다.
5. 검증 스크립트에 새 DOM 조회 방식과 중복 제거 함수 계약을 추가한다.
6. 정적 검증과 Git diff 검사를 통과한 뒤 기능 커밋을 만들고 `history.html` 최신 이력에 실제 해시를 연결한다.

### 성공 기준

- 알려진 ID 요소는 초기 바인딩 후 렌더링·이벤트 코드에서 다시 조회하지 않는다.
- 프로필·오늘 학습 필드의 저장·복원, 단계·세션 체크, 문제집 답안, 챕터 이동과 전체 코스 펼치기·접기 동작이 유지된다.
- JavaScript 구문·DOM 연결·40개 챕터·77개 학습 파일과 변경 이력 계약 검사가 통과한다.
- `history.html`이 실제 리팩터링 커밋과 검증 결과를 기록한다.
- 강제 옵션 없이 루트 `main`을 푸시하고 로컬·원격 해시가 일치한다.

## 2026-07-27 Orca 고정 URI 실행 계획

### 목적

저장소 폴더 이름이 포함된 고정 주소 `http://127.0.0.1:4174/company-work-system/`로 취업 문제집을 제공하고, 저장소 루트의 한 명령으로 같은 주소를 Orca 브라우저에서 반복해서 열 수 있게 한다.

### 감사 결과

- 작업 시작 시 실제 루트의 `main`, `origin/main`, `HEAD`는 `9e51eb9`로 일치하고 작업 트리는 깨끗하다.
- 등록된 Git worktree는 실제 저장소 루트 하나뿐이다.
- 최초 감사 뒤 `4173` 포트에서 다른 저장소 `accountProgram`의 서버가 시작된 것을 확인해 해당 프로세스를 보존하고 이 저장소는 충돌 없는 `4174`를 사용한다.
- 기존 저장소에는 고정 URI 서버나 Orca 실행 스크립트가 없다.
- `python3 scripts/verify_workbook.py` 기준 검증은 대표 이력 19개와 전체 워크북 계약을 통과한다.

### 작업 범위

1. 저장소 폴더 이름을 URL 접두사로 사용하는 로컬 전용 정적 파일 서버를 추가한다.
2. 서버가 없으면 시작하고 있으면 재사용한 뒤 Orca 공식 CLI로 고정 URI를 여는 실행 스크립트를 추가한다.
3. 루트 경로와 다른 폴더 경로는 공개하지 않고 워크북 폴더 URI만 제공한다.
4. README에 고정 URI와 실행 명령, 포트 충돌 처리 방법을 기록한다.
5. 검증 스크립트에 서버·Orca 실행 계약을 추가한다.
6. 정적 검사, 셸 구문, HTTP 성공·리다이렉트·404와 실제 Orca 탭 제목·URL을 검증한다.
7. `history.html`을 실제 기능 커밋에 연결하고 루트 `main`을 푸시한다.

### 성공 기준

- `./scripts/open_workbook_in_orca.sh` 한 명령으로 Orca에 고정 URI가 열린다.
- 같은 명령을 다시 실행해도 서버를 중복 시작하지 않고 같은 URI를 사용한다.
- `/company-work-system`은 trailing slash 주소로 이동하고 `/company-work-system/`은 문제집을 반환한다.
- `/`과 다른 폴더 경로는 워크북 또는 상위 디렉터리 목록을 노출하지 않는다.
- Orca 탭의 제목이 `Company Work System 취업 워크북`이고 로드 오류가 없다.
- 전체 워크북 검증과 Git diff 검사가 통과하며 로컬·원격 `main`이 일치한다.

## 2026-07-28 워크북 UI·화면 전환·UX 폐쇄 루프 계획

### 목적

고정 URI의 취업 문제집을 데스크톱·모바일·키보드 환경에서 직접 사용하며 화면 탐색, 챕터 선택, 문제 풀이 전환과 상태 피드백의 마찰을 제거하고 같은 회귀 검사를 반복 가능하게 만든다.

### 기준선과 재현 문제

- 작업 시작 시 실제 루트의 `main`, `origin/main`, `HEAD`는 `661ad5c`로 일치하고 작업 트리는 깨끗하다.
- `python3 scripts/verify_workbook.py` 기준 검증은 대표 이력 20개와 전체 워크북 계약을 통과한다.
- 데스크톱 1967×1179에서 가로 넘침은 없고 첫 화면의 핵심 행동과 진행 현황은 한 화면 안에 표시된다.
- 모바일 390×844에서 가로 넘침과 화면 밖 컨트롤은 없지만 고정 메뉴가 3줄, 116.5px 높이로 본문 공간을 과도하게 차지한다.
- 모바일에서 `내 목표`를 누르면 섹션 제목 상단이 0px에 놓여 116.5px 고정 헤더 뒤에 완전히 가려진다.
- `오늘 볼 챕터`에서 10장을 선택해도 선택 챕터 문제 패널은 현재 화면보다 약 1,160px 아래에 있어 전환 결과와 다음 행동을 즉시 찾기 어렵다.
- 일반 버튼에는 사이트 공통 `:focus-visible` 표시가 없고 모션 축소 환경에서도 부드러운 스크롤과 전환이 유지된다.

### 작업 범위

1. 고정 헤더 높이를 고려한 섹션 앵커 여백을 추가한다.
2. 모바일 상단 메뉴를 한 줄 가로 탐색으로 바꾸고 현재 앵커 메뉴를 표시한다.
3. 챕터 선택 필드 바로 아래에 선택 문제로 이동하는 명시적 버튼을 제공한다.
4. 이전·다음 챕터 전환 뒤 선택 챕터 제목에 포커스를 이동해 화면 변화가 전달되게 한다.
5. 모든 버튼의 키보드 포커스 표시와 `prefers-reduced-motion` 대응을 통일한다.
6. 검증 스크립트에 앵커·메뉴·챕터 이동·접근성 계약을 추가한다.
7. 데스크톱·모바일·키보드 상호작용과 저장·초기화 회귀를 반복 검증한다.
8. README와 `history.html`을 실제 커밋·검증 결과에 맞춰 갱신하고 루트 `main`을 푸시한다.

### 성공 기준

- 390×844에서 상단 헤더는 90px 이내이고 메뉴는 한 줄로 탐색된다.
- 모든 내부 메뉴 이동 뒤 대상 제목의 상단이 고정 헤더 아래에 완전히 보인다.
- 챕터를 선택하면 문제 이동 버튼이 활성화되고 한 번의 조작으로 선택 문제 제목이 헤더 아래에 보이며 포커스를 받는다.
- 이전·다음 챕터 전환은 선택 값, 제목, 문제, 위치와 포커스를 함께 갱신한다.
- Tab 키로 모든 버튼의 포커스 위치를 구분할 수 있고 모션 축소 환경에서는 부드러운 스크롤과 이동 애니메이션이 비활성화된다.
- 기존 입력 저장, 단계·세션·챕터 완료, 힌트·정답, 펼치기·접기와 오늘 기록 초기화가 유지된다.
- 데스크톱 1967×1179·모바일 390×844에서 문서 가로 넘침이 없고 브라우저 오류 로그가 0개다.
- 전체 정적 검증과 Git diff 검사가 통과하며 로컬·원격 `main`이 일치한다.

### 최종 로그 보완 루프

1. 메인·이력 HTML에 자체 포함 SVG 파비콘을 지정한다.
2. 검증 계약과 실제 HTTP 요청 로그로 `/favicon.ico` 404 제거를 확인한다.
3. 보완 내용을 의미 단위로 커밋하고 루트 `main`을 다시 푸시한다.

## 2026-07-29 기능 워크북 열기 오류 폐쇄 루프

### 목적

- 메인 화면의 `기능 워크북 열기` 링크가 실제 브라우저에서 열리지 않는 문제를 재현하고 원인을 제거한다.
- 같은 유형의 워크북 링크가 다시 깨지지 않도록 서버·문서·브라우저 검증 계약을 보완한다.

### 작업 순서

1. 실제 링크 클릭 뒤 주소, HTTP 응답, 화면 내용과 브라우저 로그를 확인한다.
2. 링크 대상과 로컬 서버 공개·렌더링 계약을 최소 범위로 수정한다.
3. 메인·워크북 화면 전환과 관련 링크를 브라우저에서 반복 검증한다.
4. 정적 검증, 문서와 `history.html`을 갱신하고 의미 단위 커밋을 푸시한다.

### Orca 최종 보완 루프

1. 기존 Orca 탭이 있으면 새 탭 생성 대신 같은 워크트리 탭을 재사용한다.
2. 로컬 서버 응답을 매번 재검증해 오래된 메인 화면 캐시를 사용하지 않게 한다.
3. Orca 공식 CLI로 메인 화면의 기능 워크북 링크를 실제 클릭하고 최종 제목을 확인한다.

### 성공 기준

- `기능 워크북 열기` 한 번의 클릭으로 읽을 수 있는 워크북 화면이 열린다.
- 이동한 주소가 고정 폴더 URI 아래에 있고 HTTP 오류가 없다.
- 뒤로 가기와 다시 열기가 정상이며 브라우저 경고·오류 로그가 0개다.
- 기존 문제집, 챕터 이동, 저장 상태와 Orca 고정 URI 계약이 유지된다.
- 전체 검증이 통과하고 로컬·원격 `main`이 일치한다.

## 2026-07-29 코드·힌트·정답 통합 문제 페이지 정리

### 목적

- 40개 문제를 별도 원문이 아니라 실제 풀이에 사용하는 전용 문제 페이지로 제공한다.
- 각 문제에서 해당 Starter·Hard·워크시트 파일을 바로 열고, 같은 카드에서 힌트와 정답을 확인하게 한다.
- 문제·해설·전체 목차의 기존 원본을 재사용해 한쪽만 수정되어 생기는 중복과 불일치를 막는다.

### 작업 순서

1. `problems.md`, `answers.md`, `index.html`의 00~39 번호와 파일 링크 대응을 검증한다.
2. 전용 문제 페이지에서 세 원본을 불러와 40개 문제·힌트·정답·실제 파일 링크로 결합한다.
3. `.fragment` 파일도 공통 코드 읽기 화면에서 열리도록 문서 라우팅 범위를 맞춘다.
4. 메인·이력·직접 문제 문서 접근을 전용 문제 페이지로 통일한다.
5. 정적 계약, HTTP 이동, 데스크톱·모바일·키보드·Orca 상호작용을 반복 검증한다.
6. README와 `history.html`을 실제 커밋과 검증 결과에 맞춰 갱신하고 루트에서 푸시한다.

### 성공 기준

- 문제 페이지에 00~39 문제 카드가 순서대로 40개 표시된다.
- 모든 카드에 힌트와 같은 번호의 정답이 있고 정답을 카드 안에서 펼치고 접을 수 있다.
- 모든 카드에 실제 Starter·Hard 또는 서술형 워크시트 링크가 한 개 이상 있으며 파일 읽기 화면으로 열린다.
- `practice/problems.md` 직접 접근과 메인·이력의 문제 링크가 같은 문제 페이지로 연결된다.
- 390×844와 데스크톱에서 가로 넘침이 없고 키보드로 정답을 열 수 있으며 브라우저 오류 로그가 0개다.
- 전체 검증이 통과하고 로컬 `HEAD`와 `origin/main`이 일치한다.
